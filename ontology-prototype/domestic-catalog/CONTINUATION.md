# 국내 공개 메타데이터 수집 이어가기

**최신 지시와 판정 — 2026-09-15 KST:** 사용자가 “Codex가 충분하다고 판단하면 중지해도 된다”고 위임했다. [충분성 판단](COLLECTION-SUFFICIENCY.md)에 따라 프로토타입의 개념·관계 설계를 시작할 메타데이터는 충분하므로 **대량 자동 수집을 종료한다.** 아래 과거 이력의 계속 수집·자동 재개 지시는 이 최신 정책으로 대체한다.

- 현재 정책: `completion-policy.json` v2.0. `prototype_metadata_sufficient: true`, `bulk_collection_enabled: false`, `automatic_resume_allowed: false`.
- 전국 전수 수집·전체 칼럼 수집·온톨로지 의미 검증 완료는 모두 아니다. 큐의 remaining·실패·미확인 상태는 그대로 보존한다.
- 예약 자동화 `automation`은 삭제했다. 수집기·인덱서·감시자는 기존 `.stop` 표시를 통해 처리 중인 작업을 마친 뒤 종료한다. 최종 상태는 `collection-stop-report.json`과 최신 `workers-*-check.json`을 따른다.
- `.stop` 표시를 지우거나 `resume_runtime.ps1`로 수집을 다시 시작하지 않는다. 이 스크립트와 supervisor에도 충분성 정책에 따른 재개 차단을 추가했다.
- 검색용 `browse.py`와 기존 지식그래프는 유지한다. 다음 과제는 88개 초안 개념 및 703개 선별 매핑 검토 항목의 뜻·단위·식별체계·적용 조건을 정리하는 것이다. 사람 승인 없이 의미 관계를 확정하지 않는다.
- 관계 검토 중 특정 근거가 부족하면 해당 출처와 명세를 한정해 보충한다. 목록이 남았다는 이유로 전체 수집을 재개하지 않는다. 추가 Git 반영 요청이 없으므로 이번 정책·수집 변경은 공개하지 않는다.

## 과거 수집 운영 방법 (현재 자동 재개 금지)

- 저장소: `C:/Users/alsw0/Desktop/project1`. 다른 프로젝트에 접근하거나 변경하지 않는다.
- 파일 정의 수집 명령: `python -X utf8 ontology-prototype/domestic-catalog/collect_definitions.py --all --workers 3`.
- 2026-09-13 07:06 UTC에 시작한 최초 프로세스 PID는 28428이다. PID 재사용 가능성이 있으므로 **실행 파일·명령행까지 확인**한다. 후속 워커는 `.local/domestic-catalog/workers.json`을 확인한다.
- 로그: `.local/domestic-catalog/*.stdout.log`, `*.stderr.log`. 진행: 이 디렉터리의 `*-collection-report.json`.
- 정상 동작 중인 워커를 중복 실행하지 않는다. 중단되면 저장된 정의 JSON을 재사용하여 이어간다. Windows 백그라운드 실행에는 `Start-Process -WindowStyle Hidden`을 사용한다.
- 이 작업에 30분 간격으로 돌아오는 heartbeat가 설정되어 있다. 기존 자동화 ID는 `automation`이며 중복 생성하지 않는다. 새 제공처 어댑터 개발도 계속해야 한다. 워커 상태 확인만으로 작업을 끝내지 않는다.
- 현재 워커 명령과 PID는 `.local/domestic-catalog/workers.json`을 먼저 확인한다. `collect_kosis.py`, `collect_seoul.py`, `collect_data_go_api.py`, `collect_data_go_std.py`, `collect_assembly.py`, `collect_culture.py`가 추가되었다. `index_progress.py --watch`가 5분마다 새 정의를 검색 DB에 추가한다. PID가 없거나 명령이 다른 경우에만 해당 큐를 재개한다.
- `supervise.ps1`이 60초마다 이 프로젝트의 워커를 확인하고, 큐가 끝나기 전에 종료한 워커만 재실행한다. 현재 감시 PID는 `.local/domestic-catalog/workers.json`의 `supervisor_pid`를 확인한다. 예약 후속 실행은 감시 프로세스가 살아 있는지도 확인해야 한다. 큐가 소진된 워커는 재실행하지 않으므로 실패 항목 재해석·다른 목록 확장은 후속 작업이 계속 담당한다.

## 저장 구조와 주의점

- `evidence/`: 공개 원문 gzip 및 URL·조회일·SHA256 영수증. HTTP 200도 오류 화면일 수 있다.
- `definitions/`: 포털/등록 식별자별 명세. `human_approved: false`는 실제 데이터 분석 및 사람 승인을 아직 수행하지 않았음을 뜻한다.
- `inventory/`: 휴대 가능한 목록·칼럼 JSONL gzip. 대량 원시 관측값을 받는 작업이 아니다.
- `.local/domestic-catalog/catalog.sqlite3`: 검색용 인덱스. `build_inventory.py`는 기본 3개 벌크 목록으로 DB를 다시 만들므로 새 제공처와 새 칼럼이 있는 DB 위에 무심코 재실행하지 않는다.
- `finalize.py`는 전체 정의를 다시 인덱싱한다. 진행 중 워커의 최신 결과가 `coverage-report.json`과 아직 다를 수 있으므로 생성 시간을 확인한다. 새 포털·API 종류를 추가하면 인덱서와 검증기를 먼저 확장한다.
- 확장 후의 기본 반영 도구는 `index_progress.py`다. `finalize.py`는 이전 초기 5개 포털용 코드이므로 현재 작업에서 실행하지 않는다. `schema_documents`와 `schema_document_sources`가 정의 파일의 증분 인덱싱을 관리한다. `catalog_storage.py`는 새 포털의 카탈로그를 다른 행을 지우지 않고 추가한다.
- API 요청 인자, 출력 칼럼, 통계 분류, 측정 항목, 분류 코드의 일부 표본을 구분한다. 같은 이름이라고 같은 개념이나 조인 가능 관계로 확정하지 않는다.
- 원문에 없는 기관·라이선스·단위·품질 점수를 추측하지 않는다. 인증·접근 제한을 우회하지 않으며 429 등에서는 해당 호스트 속도를 낮추고 대기한다. 다른 공개 출처의 작업은 계속할 수 있다.
- 현재 요청은 수집 계속이다. Git 커밋·푸시·PR 또는 다른 사람에게 메시지를 보내는 권한으로 확대하지 않는다.

## 우선 이어갈 작업

1. 공공데이터포털 FILE 83,691개 등록 키 전체의 정의 수집을 유지한다. 정의 없는 화면은 공개 파일 헤더·원기관 명세 등 대체 경로를 조사한다.
2. KOSIS 국내 목록의 orgId/tblId가 확인되는 모든 통계표를 순회한다. 공개 `statHtml/statHtmlContent.do`의 `g_jsonStatInfo`에는 분류, 항목, 단위, 기간이 있다. 기본 화면에 실린 분류 코드는 전체 코드 목록과 다를 수 있다.
3. 서울 목록 전체와 공공데이터포털 API의 모든 연산별 출력 명세를 수집한다. 정상 웹 UI의 공개 조회 요청을 근거로 어댑터를 만든다.
4. 경기·NEIS API 외 파일과 나머지 국내 제공처로 확장한다. 현재 등록 후보 목록은 확정 전국 전수 목록이 아니다. 공식 디렉터리의 신규 제공처도 조사한다.
5. 검증은 목록 총계 대조, ID 중복·누락, 원문-칼럼 대응, 출처 참조, 미해결 상태 보존을 포함한다. 의미 관계 검증과 실제 값의 상관분석은 이 수집과 별도 단계다.
6. `build_frontier.py`로 공공데이터포털 상세 페이지의 외부 참조를 추출한다. 라이선스 링크를 자료 링크와 구분하며 동일 자료라고 단정하지 않는다. `inventory/provider-discovery-frontier.json`의 새 호스트를 조사한다. 영등포·방송통계포털은 공개 입구를 확인해 등록부에 추가했다.
7. 열린국회 OpenAPI 278개, 문화데이터광장 OpenAPI 468개는 목록 총계와 고유 ID 수가 일치했다. 해당 포털의 파일·일반 정보공개·소속기관 별도 API는 아직 별도 큐다. API 명세 큐 소진 후 그 범위로 확장한다.
8. 수집 진행 중에는 `coverage-report.json`이 워커 보고서의 최신 결과를 아직 포함하지 않을 수 있다. `write_report.py`로 설명문과 포털별 표를 갱신한다. `validate.py`는 출처 해시, 이름공간, 요청/출력 분리, 실재 필드, 집계 무결성을 검증한다. `fields_in_source_definition_documents`는 명세 문서별 항목 합계이고 `documented_column_occurrences`는 표준 명세를 제공기관별 등록정보에 연결한 반복까지 포함하므로 둘을 구분한다.
9. `collect_culture_files.py`가 문화데이터 파일 목록 470개와 CSV 첫 레코드만 수집한다. `header_candidates`는 공식 칼럼 명세로 승격하지 않는다. 헤더 후보도 검색 가능하고 별도 압축 파일로 내보낸다. 파일 원문 전체의 SHA256과 첫 레코드의 SHA256을 혼동하지 않는다. HTTP Range가 무시되어도 첫 레코드(최대 64 KiB)만 읽고 연결을 닫는다. 현재 문화 CSV에는 API에 없는 `RNUM` 등도 있어 API와 파일 스키마를 자동 동일시하지 않는다.
10. KOSIS 파서 2판은 항목의 영문 이름에 작은따옴표가 있는 원문도 JSON 객체 경계로 안전하게 읽는다. 이전 파서 실패 108건이 저장된 원문 재해석 검사에서 복구되었다. 재실행 시 이전 파서의 실패 결과만 다시 해석하도록 구현했다. 문화 API의 일시 연결 종료 1건도 별도 재시도 영수증으로 복구하여 468개 API 모두 출력 명세를 확보했다.
11. `collect_ecos.py`가 ECOS 공개 통계코드검색 UI의 익명 조회를 재현한다. 공개 프런트엔드의 `OSUUA01R01/R02/R03` 읽기 트랜잭션으로 분류 트리·표별 분류·분류 코드를 조회하며, 실제 통계 관측값은 요청하지 않는다. 트리 940개 중 통계표 681개와 분류 노드 259개를 구분한다. 코드별 단위와 상위 코드를 보존하고 응답의 dataCcnt와 수신 개수를 대조한다. 코드 목록 수를 칼럼 수로 합산하지 않는다.
12. `collect_yeongdeungpo.py`가 영등포구 포털의 자체 목록과 공개 명세를 수집한다. 목록 153개(20페이지), API 150개와 다른 유형 3개다. 원문에서 제공기관과 제공부서가 다를 수 있으므로 둘을 별도 보존한다. 로그인·약관 팝업의 데이터 제공 기능은 사용하지 않고 공개 문서만 읽는다. 서울 포털과 OA 코드가 같더라도 동일 자료·같은 기준일이라는 관계는 아직 확정하지 않는다.
13. 국내로 제한한 것은 **포털 제공처**다. 국내 포털에 공개된 국제 통계까지 주제만으로 제외하는 것으로 확대하지 않는다. 현재 KOSIS MT_ZTITLE만 확보했으므로 국제·다른 공개 목록 뷰도 공식 목록 경로를 확인한 뒤 추가 수집 대상에 포함한다. OECD/World Bank 독립 포털은 현재 제공처 범위 밖이다.
14. `collect_incheon.py`가 인천 공개 등급 5 목록을 UI와 같은 POST `/api/data/public`에서 수집한다. 현재 표시 4,382건, 10개씩 439페이지다. 각 요청이 약 20초 걸릴 수 있다. 5페이지마다 중간 저장하므로 초기 수량을 전체로 보고하지 않는다. `srcSe/dataId` 복합 식별자를 사용한다. 상세 `/api/data/detail`의 `apiInfo.responseParamNm`은 펼친 문자열이므로 `declared_parameter_lists`에 후보로 보존하고 명세 칼럼 수에 넣지 않는다. 고유 `openapilist.operationId`가 있으면 공개 `/api/data/detail/api`의 `responseInfo`를 읽는 경로가 구현되어 있으나 실재 원문을 확보하기 전에는 해당 경로의 전체 동작을 검증했다고 주장하지 않는다. `dataGrd`는 사이트 공개 조회 조건이지 프로젝트가 부여한 품질 점수가 아니다.
15. `collect_daegu.py`는 공개 HTML `/open/data/dataList.do`의 `var dataSetListInfo`를 JSON 구문으로 읽는다. 전체 데이터셋 상세 등록 19,954건, 9개씩 2,218페이지를 순회한다. 최신순은 원문 UI에 있는 `detailRegisterDate DESC, dataSetDetailId DESC`를 사용한다. `dataSetId` 하나에 월별·자료별 `dataSetDetailId`가 여럿 있으므로 상세 ID를 등록 키로 보존한다. API 별도 필터 14건을 먼저 확보했다. `/data/rest/...` 직접 요청은 로그인 페이지로 이동하여 사용하지 않았으며, 공개 `/open/...` 목록에서 동일 페이지 파라미터가 적용됨을 확인했다. 별도 외부 LINK 목록과 파일·API 원기관의 칼럼 명세는 남은 범위다.
16. 인천·대구 수집기도 `workers.json` 및 `supervise.ps1`에 연결되었다. 수집 중 목록 보고서에는 `snapshot_pagination_complete: false`가 정상이다. `build_frontier.py`가 두 포털의 원문 `pageUrl`/`dataUrl`도 출처와 함께 `referencesExternalPage`로 내보낸다. 외부 참조 ID에는 출처 포털을 포함해 포털 간 같은 숫자 ID 충돌을 막는다. CSV 헤더와 펼친 API 변수 목록은 검색 가능하되 명세가 확인된 자료 필터에는 들어가지 않는다.
17. ECOS의 681개 통계표 분류 명세 큐가 끝났다. 분류 항목 1,182개와 분류별 코드 출현 38,957개를 보존했다. 679개 표는 공개 응답의 개수가 맞고, `903Y003`·`903Y203`의 `GUBUN_CD` 코드 조회는 HTTP 400으로 미해결이다. 프런트엔드와 같은 분류 객체 및 dsId 요청인지 저장된 JS와 대조했으며, 이름만 보고 다른 코드집을 대신 연결하지 않았다. [코드 수집 대조 보고서](ecos-schema-coverage-report.json)를 확인한다. 코드 수를 칼럼 수에 더하거나 681개 표 모두 코드까지 완료했다고 보고하지 않는다.
18. 2026.09.13 후속 검증에서 37개 검사가 통과했으며, 이 시점 원문 25,111개 해시를 대조했다. [페이지 수집 중지·재개용 작은 큐 검사](pagination-adapter-check.json), [실제 HTTP 검색 검사](browser-adapter-check.json)도 통과했다. 이후 들어온 원문까지 자동으로 같은 검사를 통과했다고 확대하지 않는다. `ordered_pages`가 페이지 요청을 최대 워커 수의 두 배만 예약하므로 중단 시 수천 개 요청이 대기하지 않는다. 검증용 임시 stop 표시는 제거했고, supervisor가 새 인천·대구·인덱서를 재개한 것을 확인했다.
19. 대구 데이터셋 상세 목록은 2026.09.13 09:40 UTC에 2,218페이지·19,954행·19,954개 고유 상세 ID가 일치하여 해당 목록 스냅샷의 페이지 수집이 끝났다. 실패 페이지와 중복 관측은 없다. 이 큐를 다시 처음부터 실행하지 않는다. 별도 외부 LINK 목록과 각 상세 자료의 원기관 API·파일 명세는 여전히 남아 있다.
20. `openapi_schema.py`와 공공데이터포털 API 파서가 화면에 내장된 Swagger/OpenAPI를 읽는다. 최초 저장 원문 재해석에서 255개 명세·13,254개 응답 항목을 복구했다. [최초 보고서](data-go-api-swagger-first-recovery-report.json)에 literal `undefined`로 실패한 24건도 남아 있다. 참조 위치 보강 후 [검사](new-adapter-check.json)에서는 당시 확보한 명세 259개·13,359개 응답 항목 전체를 원문 JSON 포인터와 대조했다. 둘은 중복되는 시점별 수량이며 더하지 않는다. 최신 검사 이후 수집분은 자동으로 검사 완료라고 확대하지 않는다. JavaScript를 실행하지 않으며 외부 명세 참조·순환 참조·동적 속성은 미해결로 남긴다. 요청 인자·응답 코드·매체별 변형을 보존하고 모두 분석 변수라고 주장하지 않는다. `schema_parser_version`으로 오래된 내장 명세 파서를 재해석하며, `--cached-swagger-only`는 HTTP 없이 저장한 원문만 읽는다.
21. `collect_library.py`가 도서관 정보나루 공개 API PDF 매뉴얼 v20260210(70페이지, evidence `library-api-manual`)을 수집했다. 번호가 붙은 서비스 19개, 고유 호출 URL 18개, 응답 요소 436개다. 서비스 4·5는 같은 URL을 쓰지만 추천 유형이 달라 별도 등록으로 보존한다. 서비스 15의 URL은 PDF 표의 여러 칸으로 분리되어 있으므로 합친 뒤 45페이지 화면과 대조했다. 원문 페이지·표·행·셀 좌표를 저장하고 중첩 경로는 추측하지 않는다. 요청 인자와 분류 코드표는 별도 파일/배열에 두며 응답 요소 수에 합산하지 않는다. 매뉴얼 버전이 바뀌면 번호 범위를 다시 확인하도록 파서가 중지한다. 웹 API 안내에는 18개, PDF에는 신착도서까지 19개 서비스가 있다. 도서관별 다운로드 목록·테마자료와 해당 명세는 후속 수집 범위다. PDF 의존성은 이 폴더의 `requirements.txt`에 추가했다. 실행 검증에는 `C:/Users/alsw0/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe -X utf8 ontology-prototype/domestic-catalog/collect_library.py`를 사용했다. 이 매뉴얼 큐는 끝났으므로 supervisor에 반복 실행 워커로 추가하지 않았다.
22. 2026.09.13 10:00 UTC 인덱스 스냅샷에는 포털 12곳·등록정보 412,534행·명세 문서별 항목 415,511개가 반영되었다. 이후 수집분은 계속 늘어난다. 후속 전체 검사 42개가 통과했고 원문 35,913개의 SHA256을 대조했다. [수집 검사](validation-report.json), [새 어댑터 검사 11개](new-adapter-check.json), [HTTP 검색 검사 5개](browser-new-adapter-check.json)를 참고한다. 도서관 BookCount 및 여행경보 iso_code 검색을 확인했고 authKey는 응답 명세 필터에 나오지 않았다. 검증 동안 인천 목록과 인덱서만 잠시 멈추었으며, 이 검증용 stop 표시는 제거했다. 다른 수집 워커는 계속 동작했다.
23. 위 검증 뒤 감시 프로세스가 인덱서 PID 16164와 인천 PID 9492를 재개했고, API 파서는 PID 1748로 실행 중임을 명령행과 대조했다. FILE·KOSIS·서울도 각각 한 프로세스이며 중복 워커가 없다. stop 표시는 남아 있지 않다. PID는 당시 확인값이므로 다음 실행에는 workers.json과 실제 명령행을 다시 확인한다. [이번 확장 기록](continuation-20260913T1000-report.json)을 남겼다.
24. 인천은 10:20 UTC에 공개 등급 5의 439페이지·4,382행·4,382개 고유 복합 ID가 일치했다. 현재 워커는 자료별 상세 명세 큐를 진행한다. 처음 확보한 자체 도서관 API `7661IVAUC0E47D7A30/76F8I2ID524F3448E0`의 연산 `76F89QQJ77B86A5B80`에서 responseInfo 26개를 원문과 대조했다. LBRRY_NM과 varchar(100) 타입, 요청 인자 분리가 [새 검사](finance-address-adapter-check.json)를 통과했으므로 이 실재 원문에 한해 native operation 경로의 동작을 확인했다. 다른 원기관의 펼친 apiInfo 문자열은 계속 후보 상태다.
25. `collect_opendart.py`가 OpenDART의 공개 개발가이드 6개 분류 전체에서 API ID 85개를 확보했다. 별도 소개 게시판은 9페이지·83개 게시글이며 두 목록 수와 일부 제목이 다르다. API ID와 게시글 ID를 동일시하지 않는다. 모든 가이드 페이지를 읽어 응답 항목 2,175개를 저장했고, result/list 구조 노드는 별도 배열이다. 83개는 명명된 응답 필드를 확보했으며 2019003(공시 원문)·2019019(XBRL 원문)는 오류 응답 두 항목만 있으므로 파일 내부 명세는 미해결이다. 관측값 API와 원문 ZIP을 호출하지 않았다. [목록 대조](opendart-catalog-report.json), [진행](opendart-collection-report.json)을 참고한다. 소개 상세 4138도 정상 공개 form POST로 읽었으나 API ID 연결은 없었다. 이름만으로 소개-가이드 관계를 확정하지 않는다. 재무정보·XBRL·주석 일괄 다운로드의 별도 목록과 파일 명세는 남아 있다.
26. `collect_address.py`는 주소기반산업지원서비스의 공개 Vue 배포 파일에서 UI getApiList()의 정적 배열 전체를 읽는다. 11개 uniqKey와 11개 가이드 경로를 대조하고 9개 서비스의 응답 항목 199개를 확보했다. JavaScript를 실행하지 않고 문자열 리터럴의 HTML만 파싱한다. 원문 문자 시작·끝, 표·행, 원래 셀과 rowspan 확장을 기록한다. useDetail<br>Addr만 HTML 줄바꿈을 제거해 useDetailAddr로 읽되 원문 표기를 함께 남겼다. currentPerPage 등 원문의 이름은 임의 수정하지 않는다. 상세주소 팝업은 다른 API에 통합하는 예시만 있고 독립 출력 표가 없어 미해결이다. getApiList의 상세주소 항목을 다른 API와 합치거나 삭제하지 않는다.
27. 주소 지도 검색 API의 공개 다운로드 가이드 ZIP 2,379,582바이트를 읽었다. 실제 데이터 파일이 아닌 매뉴얼과 예제이며 내부 코드는 실행하지 않았다. HTTP Content-Type은 application/json이지만 실제는 ZIP인 차이를 QA로 기록했다. PDF 멤버만 크기 확인 후 메모리에서 읽어 별도 파생 영수증 `address-map-guide-pdf-2021`에 ZIP 출처·멤버명·PDF 해시를 연결했다. `collect_address_map_manual.py`는 29페이지 매뉴얼의 10페이지 입력 14개를 추출했으며 출력 칼럼을 추가하지 않았다. PDF 10페이지 화면과 대조했고, Keyword와 예시의 keyword는 원문 차이로 남겼다. 실행 시 PDF 라이브러리가 있는 번들 Python을 사용한다. 주소 DB·공간정보·코드 자료의 목록은 아직 미수집이다. 공개 JS `JstAddressDownload-c5f05456`에는 읽기용 selectBabsRtlDtaDtlList / selectAttrbDBDwldList 경로가 있으나 ID·목록 범위와 공개 접근 조건은 추가 조사해야 한다. 장바구니·신청·인증 경로는 사용하지 않는다.
28. 이번에 완료된 OpenDART·주소 API 가이드 큐를 supervisor에 반복 워커로 추가하지 않았다. 저장된 원문과 명세를 재사용하므로 재실행해도 네트워크 조회를 되풀이하지 않는다. 주소 파서 2판은 map manual의 후속 주석을 보존한다. [금융·주소 원문 QA](finance-address-source-qa.json)에 목록 수 차이, total_count의 이름/설명 차이, 다운로드 MIME 차이, Keyword 대소문자 차이를 기록했다. 실제 데이터 결측률·정확도나 품질 점수를 산출한 것은 아니다.
29. 다음 확장용으로 공개 UI JstAddressDetailsSearch의 읽기 요청 POST /api/jst/selectBabsRtlDtaDtlList {}를 그대로 조회해 주소 DB 분류 메타데이터 31건·고유 ID 31개를 확보했다. 원문 address-db-catalog-public 및 inventory/address-db-catalog-probe.json에 저장했고 아직 카탈로그 DB 수량에는 넣지 않았다. RLS_YN Y/N을 모두 원문대로 보존했으며 메타데이터 공개를 파일 접근 권한으로 확대하지 않는다. [발견 보고서](address-db-discovery-report.json)를 읽고 API 11개와 별도 db/ 이름공간으로 수록한 뒤 공개 구조 명세를 확장한다.
30. 10:50 UTC 인덱스 스냅샷은 포털 14곳·등록정보 413,612행·명세 문서별 항목 491,326개다. 기관별 표준 명세 반복을 포함한 출력 항목 출현은 740,158개로 두 수를 더하지 않는다. 전체 검사 48개가 통과했고 원문 45,183개의 해시를 대조했다. 새 어댑터 검사 11개와 HTTP 검색 검사 6개도 통과했다. 검증용 인덱서 stop 표시를 제거했으며 다른 수집 워커는 계속 동작했다. [이번 확장 기록](continuation-20260913T1050-report.json)을 확인한다. 이후 수집분까지 자동으로 이 검사를 통과했다고 주장하지 않는다.
31. 검증 뒤 supervisor가 인덱서를 PID 31152로 재개했으며 명령행을 확인했다. FILE·KOSIS·서울·공공데이터 API·인천 워커도 각각 하나만 실행 중이다. 임시 stop 표시가 없고 새 인덱서 오류 로그도 비어 있다. 다음 후속 실행은 최신 workers.json과 실제 명령행을 다시 확인한다.
32. `collect_address_db.py`가 공개 `selectBabsRtlDtaDtlList`의 31개 분류와 `JspAddrInfoExperience-14b37ad8.js`의 명시적인 RTL_DTA_DTL_SN → Schema_*.js 분기를 대조했다. API 11개와 별도 `db/<분류ID>` 이름공간으로 31개를 인덱싱했으며 주소 포털 등록은 현재 42개다. 파서 3판에서 31개 분류의 칼럼 출현 1,117개를 확보했다. 원문 자료형·크기·PK를 그대로 저장하고 비어 있으면 추측하지 않는다. JS를 실행하지 않는 `static_js_data.py` 리터럴 파서를 사용한다. `DataSample_*.js`의 예시는 공식 스키마 대신 사용하지 않는다. [분류 목록](address-db-catalog-report.json), [스키마 수집](address-db-collection-report.json)을 확인한다. 이 31개 큐는 끝났으므로 supervisor 반복 워커가 아니다.
33. 주소 DB의 한 분류에 여러 형식이 있을 수 있다. `db/1`은 24개·14개 칼럼 배열을 별개 형식으로 보존했고 `db/25`는 rowSpan 그룹 11개에 각각 4개 칼럼을 보존했다. `db/30`의 기관 종류 배열은 칼럼 9개에 더하지 않는다. `RLS_YN` Y 13개·N 18개를 보존하되 공개 메타데이터와 공개 스키마를 파일 접근 권한으로 해석하지 않는다. [새 어댑터 검사](address-db-seoul-adapter-check.json)에서 1,117개 칼럼 전체의 원문 객체·문자 위치와 형식 경계, 임의 표현식 거부를 검사했다.
34. 서울 최초 8,272개 큐는 11:14 UTC에 끝났다. API 명세 5,632개·일반 시트 33개, 서비스 선언 없는 540개, 기타 미해결 2,067개다. 후자 중 1,777개는 통계형 S/2여서 `collect_seoul_statistics.py`를 추가했다. 원래 페이지 form#frm → statSheetView.do → 명시된 stat.eseoul.go.kr iframe 순서로 공개 문서만 읽는다. iframe의 orgId/tblId와 응답 hidden ID를 대조하고 원래 OA 등록 ID를 유지한다. `seoul-statistics-*.json`은 기존 실패 기록을 지우지 않고 추가 명세로 인덱싱된다. 분류·측정항목·시점을 구분하고 화면에 처음 표시된 코드 목록은 불완전으로 남긴다. 관측값 조회·다운로드 요청은 호출하지 않는다.
35. 서울 통계형 어댑터의 첫 6개 자료·23개 항목은 실제 HTML 위치 및 iframe 링크와 대조했고 다른 표 ID 응답 거부도 검사했다. OA-996에는 에너지종류별, 신재생에너지 생산량, 시점이 있고 분류 코드 4개는 전체 코드가 아니다. 1,777개 전체 큐를 PID 11524로 시작했으며 `workers.json`과 supervisor 허용 목록에 추가했다. 허용 목록 반영을 위해 supervisor만 PID 20928로 재시작했다. 실제 PID는 다음 실행에 다시 검증한다. 기존 FILE·KOSIS·API 워커는 계속 동작했다. 인천도 11:35 UTC에 4,382개 상세 큐를 소진했고 자체 API 7개·170개 명세 항목, 원기관 명세 등 미해결 4,375개를 남겼다.
36. 11:48 UTC 인덱스 스냅샷은 포털 14곳·등록정보 413,643행·명세 문서별 항목 573,460개다. 기관별 반복을 포함한 출력 항목 출현은 822,292개로 별도 수다. 전체 검사 51개와 원문 55,522개의 SHA256 대조가 통과했다. 새 어댑터 검사 12개, 실제 HTTP 검색 검사 5개도 통과했다. [확장 기록](continuation-20260913T1150-report.json), [수집 검사](validation-report.json), [HTTP 검색](browser-address-db-seoul-check.json)을 참고한다. 검증용 indexer.stop은 제거했으며 이후 새 수집분은 자동으로 같은 검사를 통과했다고 확대하지 않는다.
37. 다음 주소 자료 확장용으로 `probe_address_db_file_lists.py --period 202609`를 실행했다. 공개 UI의 `selectAttrbDBDwldList` 읽기 요청(rtlDtaDtlSn/year/month/expand=Y)으로 31개 분류 모두의 달력 메타데이터를 확보했다. [목록 발견](address-db-file-calendar-discovery-report.json), [원문 대조](address-db-file-calendar-qa.json), `inventory/address-db-file-calendar-202609.json`을 확인한다. `isExist=Y` 771행 중 파일명이 있는 출현은 619개이며, Y만으로 파일 식별자를 확정하지 않는다. N 366행은 달력 자리표시자다. 월별 목록은 연 단위, 일별 목록은 여러 월이 겹쳐 나오므로 이 수량을 중복 제거된 파일 수나 전체 역사 범위로 보고하지 않는다. db/26의 vaildDate는 201601~202608이고 다른 30개 응답에는 해당 범위가 없다. JS 달력의 과거 조회 범위와 명시적 파일 ID를 조사해 날짜별 목록 수집기를 확장한다. 아직 개별 파일을 카탈로그 DB에 추가하지 않았고 파일 본문·장바구니·신청·인증 경로를 호출하지 않았다.
38. 11:55 UTC에 supervisor PID 20928과 재개된 indexer PID 31304를 명령행으로 확인했다. FILE·KOSIS·공공데이터 API·서울 통계형 워커는 각 하나이며 stop 표시가 없다. 서울 통계형은 당시 393/1,777개·2,096항목까지 명세를 확보했다. 이후 진행은 별도 최신 보고서와 인덱스 생성 시각으로 확인한다. Git 변경 공개 없이 이 프로젝트 내부 수집을 계속한다.
39. 서울 S/2 통계형 1,777개 큐는 12:17 UTC에 소진되었으며 1,776개 자료에서 분류·측정항목·기간 8,736개를 확보했다. OA-882의 DT_201004_O020011_2020 iframe만 HTTP 500이고 12:27 UTC 후속 1회 재조회도 500이었다. 원래 실패 영수증과 `seoul-statistics-OA-882-iframe-retry1`을 보존하고 retry_attempts에 연결했다. 완료 큐를 반복 실행하지 않는다. 링크형 287개·서비스 선언 없는 540개 등 다른 서울 미해결 경로와 초기 화면에 일부만 표시된 분류 코드는 여전히 남아 있다.
40. `collect_kma_hub.py`가 기상청 API허브의 홈페이지에 연결된 13개 분야, 하위 분류 55개와 산업특화 가이드 1개를 읽었다. 공개 API 호출 예시가 있는 가이드 구획은 626개이며 출력 표 304개에서 응답 변수 출현 4,709개를 확보했다. 322개 구획은 공유 표 적용 범위·참고 PDF·격자·이미지·파일 출력 내부 명세가 미해결이다. 국내 포털의 세계기상 분류도 수집 범위에 포함한다. API 인증이나 관측값 호출을 하지 않고 공개 HTML 문서만 읽었다. [목록](kma-api-catalog-report.json), [진행](kma-api-collection-report.json)을 확인한다. 이 유한 큐는 완료되어 supervisor 반복 워커가 아니다.
41. 기상청 등록 키 `guide/<seqApi>/<seqApiSub>/<h4 위치>`는 원문 가이드 경로와 위치로 만든 프로젝트 ID다. 공식 API ID나 고유 호출 URL 수로 보고하지 않는다. 가이드 개정 시 순서가 바뀔 수 있으므로 원문 해시와 제목을 함께 대조한다. 의미(단위) 열에는 단위 외에도 코드 체계·시간대가 있어 문자열을 그대로 보존하고 unit을 임의로 채우지 않는다. 낙뢰 원시자료의 공통 요청 표 4행은 개별 제목 앞에 있어 절 수준 표로 보존했으며 각 API에 자동 상속하지 않는다. 요청인자 h4 7개는 API 구획 수에 더하지 않는다. `inventory/kma-manual-frontier.json`에 공개 참고 문서 URL 111개와 출처 위치를 수록했다. 같은 페이지의 PDF를 그 페이지 모든 호출의 명세로 확정하지 않고 후속 본문 대조가 필요하다.
42. `collect_mafra.py`가 농림축산식품 포털의 공개 검색 경로 getFileConditionSearch/getOpenapiConditionSearch/getLinkConditionSearch를 수집한다. 빈 필터 배열 []는 0개를 반환하므로 원문 전체 체크박스 값인 ["*"]를 사용한다. 수정일 정렬로 API 183개·파일 619개·링크 1,340행을 읽었으나 링크 고유 ID는 1,339개였다. UI에 있는 조회수 정렬(rdcnt)로 링크 90페이지를 추가 순회하여 두 전체 순회의 고유 ID 합집합 1,340개와 공식 총계를 대조했다. 중복과 양쪽 페이지 수·총계를 삭제하지 않았다. 현재 세 유형 고유 등록은 2,142개이며 [목록 대조](mafra-catalog-report.json)의 snapshot_pagination_complete가 참이다. 별도 표준 사전·게시물은 아직 수집 완료 범위가 아니다.
43. 농식품 원문 PUBLIC 분류 1,359개와 PRIVATE 분류 783개를 구분한다. PRIVATE는 민간·거래소 연계 목록이며 공개 /privatedata/indexPrivateDataDetail.do 화면을 사용한다. 공개 통합검색에 있다는 이유로 정부 생산 데이터나 무료 재사용 허가로 확정하지 않는다. _id(유형-data_id)를 등록 키로, dataset_id는 별도 상위 ID로 보존하고 제공기관 코드는 mafra:org: 이름공간을 쓴다. 외부 exchn_data_url과 goUrl의 URL 인자는 referencesExternalPage로만 연결한다. `build_frontier.py`도 농식품 원문 링크를 포함한다. API 응답 표의 항목·설명, 요청 인자, 빈 출력 표 문구, 예시 XML을 구분한다. 예시의 ROW_NUM·totalCnt 등을 출력 명세에 자동 추가하지 않는다.
44. 농식품 API 183개 모두의 기본 기능 공개 출력 명세 2,387개를 먼저 확보했다(기능별 후속 확장은 45번 참고). 파일·링크의 공개 상세 메타데이터 수집은 PID 16448로 계속하며 `workers.json` 및 supervisor에 등록했다. 새 허용 목록을 읽도록 supervisor만 PID 31336으로 재시작했으며 기존 FILE·KOSIS·공공데이터 API 워커는 중단하지 않았다. 현재 프로세스는 다음 실행에 PID와 명령행을 함께 확인한다. 농식품 파일 화면에는 다운로드·미리보기 경로가 있으므로 명세 없는 파일의 헤더를 후속 조사하되 본문 전체를 내려받지 않는다. [수집 진행](mafra-collection-report.json), [원문 QA](kma-mafra-source-qa.json)를 확인한다.

45. `collect_mafra_operations.py`가 183개 API의 `select#s_skll_sn`을 확인했다. 177개는 기능 1개, 3개는 2개, 2개는 4개, 1개는 6개로 선언 기능 합계 197개다. 6개 API의 20개 기능을 공식 읽기용 `/opendata/data/openDataApiDetail.do`에 data_id/api_id/skll_sn으로 조회했다. 응답 data_id 및 기능명을 선택 목록과 대조하고 19개 문서를 확보했다. `OPENAPI-20221205000000002360`의 기능 3은 최초와 retry1 모두 30초 읽기 시간 초과이며 명세를 추측하지 않는다. 기능 1·2·4의 성공한 항목 78개는 보존한다. 첫/기본 기능을 성공적으로 다시 읽은 경우 원래 기본 필드는 감사 배열에 보관하고 관측한 기능들로 교체해 중복 집계를 피한다. 최종 농식품 API 출력 항목 출현은 2,725개다. `mafra-collection-report.json`의 2,387은 먼저 실행된 기본 상세 큐가 메모리에서 집계한 수이며 후속 기능별 보고서와 더하지 않는다. 최신 인덱스 수량과 [기능별 보고서](mafra-operations-collection-report.json)를 확인한다. 반복 실행은 기존 영수증/실패를 재사용하므로 시간 초과 무한 재요청이 없다.
46. 기능 상세 HTML은 출력 표의 caption이 없고 `div#tab7`과 `a.tab7`의 출력결과 라벨을 사용한다. 이 경로를 명시적으로 확인한 표만 출력 명세로 읽는다. 기본 화면에서는 예시의 ROW_NUM을 자동 추가하지 않았지만 기능별 공식 출력 표에 ROW_NUM이 명시된 경우에는 원문 항목으로 보존한다. 출력 필드마다 기능 ID·이름·API ID·별도 영수증을 저장한다. [원문 대조 16개](kma-mafra-adapter-check.json)가 통과했고 기상청 4,709개·농식품 2,725개 전체 항목을 원래 표 셀/출처 해시와 대조했다. [가져오기 검사 5개](mafra-operation-import-check.json)는 183개 API의 저장 필드 전체, 초기 기본 명세 중복 제거, 부분 실패 유지, 실제 HTTP 상세 검색의 기능 출처를 확인했다.
47. 12:44 UTC 고정 스냅샷은 포털 16곳·등록정보 416,411건·명세 문서별 654,535항목이었다. 전체 검사 58개와 원문 68,244개의 SHA256 대조가 통과했고 [고정 검사 보고서](validation-20260913T1245-report.json)로 보존했다. 이후 기능 확장과 동시 수집분을 13:05 UTC에 다시 인덱싱했으며 그 뒤 전수 58개 검사를 다시 했다고 확대하지 않는다. 새 인덱스와 해당 단계별 검사 범위는 [이번 확장 기록](continuation-20260913T1245-report.json)에 분리해서 저장했다. 전체 검사는 원문/SQL 순회로 이번에는 약 15분 걸렸으며 오류 없이 완료됐다. 다음에는 단계별 진행 로그 등 성능 원인을 조사할 수 있으나 검사 생략으로 대체하지 않는다.
48. 농식품 FILE 공개 상세 화면의 `filePreview(data_id,file_ty_code,file_sn)`은 읽기용 `/opendata/data/previewOpenDataWebFile.do`를 호출하고 numberOfCells/listHead/listRow를 사용한다. 이번에는 이 경로를 호출하지 않았다. 후속 작업에서 원문 UI 선언과 파일 버전 ID를 대조하고 공개 미리보기의 헤더만 후보로 보존하는 어댑터를 조사한다. 본문 전체 다운로드 경로와 신청/인증은 사용하지 않는다. 기상청 참고 문서 111개, 주소 DB 과거 파일 목록, 아직 수집하지 않은 국내 포털도 후속 확장 범위다.

49. 13:08 UTC에 검증용 `indexer.stop`을 소유 문자열 확인 후 제거했고 supervisor PID 31336이 인덱서를 PID 32956으로 재개했다. FILE·KOSIS·공공데이터 API·농식품·인덱서가 각 한 프로세스임을 절대 명령행으로 대조했다. 오류 로그는 모두 0바이트이고 stop 표시가 없다. [실행 확인](workers-20260913T1308-check.json)은 당시 스냅샷이므로 다음 실행에 다시 확인한다. 13:05 인덱스는 등록정보 416,411건·명세 문서별 항목 675,090개·기관별 반복을 포함한 항목 출현 923,952개다. 새 외부 참조 큐는 17,933개 링크·라이선스 이외 호스트 471개이며 미등록 호스트의 국내 공공 제공처 여부를 아직 확정하지 않았다. 전체 수집은 계속 미완료다.

50. 농식품 기본 상세 큐 2,142개는 13:19 UTC에 소진되었다. API 183개와 명세 없는 파일·링크 1,959개를 구분하며 완료 큐를 supervisor가 다시 실행하지 않는다. 별도 기능 확장 2,725항목과 기본 큐의 2,387항목은 중복되는 시점별 수량이므로 더하지 않는다. 파일 619개의 공개 헤더/원기관 명세, 기능 20221205000000002360/3 시간 초과, 별도 표준사전 등은 남아 있다.
51. `collect_busan.py`가 부산 Big-데이터웨이브의 공개 공공데이터 카탈로그 12,551개 PUBLICDATAPK를 수록했다. 목록 다운로드 UI가 호출하는 POST `/bdip/srh/getListSearch.do`(pagelength=-1)의 JSON 메타데이터 7,648,856바이트를 읽었으며, 배열 길이·고유 키·export total·일반 검색 total_count가 모두 12,551로 일치했다. 관측값 파일은 아니다. 전체 원문과 `result[n]` 위치를 보존한다. 제공기관 코드는 busan:org: 이름공간에 두고 숫자 키가 data.go.kr ID와 같다는 이유만으로 동일성을 승인하지 않는다. 영상아카이브 BT_ 6,244건과 보고서/링크도 등록에 포함된다. 다른 카탈로그 탭·마켓·맞춤형 범위는 아직 완료가 아니다. [목록 보고서](busan-catalog-report.json)를 확인한다.
52. 부산 상세 HTML 탐색의 최초 인자 오류(uuid에 등록 ID를 넣음)를 QA에 남기고 공식 doDetail의 publicdatapk 인자로 바로잡았으나, 실제 등록 4개 표본도 HTTP 200 오류 화면이었다. uuid 빈 값 추가도 오류였다. 인증 필요로 단정하거나 전체 등록의 서버 오류로 확대하지 않는다. 원문 공개 목록에서 미리보기 버튼이 호출하는 selectDataSet/selectOpenData/selectFileData의 JSON은 정상 반환하므로 해당 경로를 수집한다. API operationSeq/listId와 요청·응답 이름/설명 문자열, 파일 버전·원문 링크를 보존한다. 큰따옴표 안의 쉼표를 처리하지만 이 문자열들은 `flattened_parameters_not_verified_schema` 후보이고 fields 배열은 비워 둔다. fileCnt=0/fileList 3개 차이도 보존한다. FD_ 관리자 파일 85개는 별도 getBigFileList 안내가 있으나 아직 그 목록 경로를 수집하지 않았다. [원문 QA](busan-ulsan-source-qa.json), [미리보기 진행](busan-collection-report.json)을 확인한다.
53. 부산은 API→SHEET→나머지 순서로 12,551개 기본 공개 메타데이터를 이어서 수집한다. 프로세스 PID 16492로 시작했고 supervisor 허용 스크립트 및 workers.json에 추가했다. 허용 목록을 읽도록 supervisor만 명령행 확인 후 PID 5872로 재시작했다. 이전 FILE·KOSIS·공공데이터 API·인덱서는 중단하지 않았다. 기존 evidence/definitions를 재사용하므로 초기 원문을 반복 요청하지 않는다. 각 API의 파라미터 후보가 길이상 맞는다고 공식 구조 명세나 관측값 검증으로 승격하지 않는다.
54. `collect_ulsan.py`는 울산 통합 목록의 HTML이 오픈데이터 구획에서 끊기는 현상을 확인한 뒤 공개 파일·API·표준 개별 탭을 모두 순회했다. 화면에서 선택할 수 있는 15개 보기를 사용했고 hidden page/mId, 선택된 페이지 크기, 마지막 페이지, 닫는 HTML을 대조한다. FILE 153페이지 2,284행, API 8페이지 116행, STD 13페이지 182행으로 총 174페이지·2,582행이다. 독립적인 울산 등록 ID가 없어 `20260913-size15/<유형>/<페이지>/<행>`을 프로젝트 스냅샷 ID로 사용한다. 추후 원문 개정은 새 snapshot namespace로 구분해야 하며 현재 키를 공식 영구 ID로 보고하지 않는다. 총 973개 원문 링크와 링크가 반복되는 그룹 458개가 있고, 제공기관/기능/버전별 목록 행을 합치지 않았다. 기존 API의 여러 기능이나 표준 제공기관별 항목을 링크 ID 하나로 축소하지 않는다. 같은 전체 페이지가 반복되지는 않았다. 다른 통계·빅데이터 게시판과 원본 명세는 미해결이며 이 유한 목록 큐는 supervisor 반복 워커가 아니다.
55. `build_frontier.py`가 부산 미리보기와 울산 행의 명시적 원문 링크를 추가한다. 부산 링크는 기본 상세 JSON이 아니라 실제 링크가 있는 selectOpenData/selectFileData 영수증과 배열 속성 위치를 사용한다. `same_dataset_asserted`와 `joinability_asserted`는 모두 false다. 기본 검색 화면에 부산·울산 이름/진행 큐를 추가했고, 후보 문자열 검색은 가능하되 검증 명세 필터에서는 제외되는지 확인했다. 새 [원문 검사 15개](busan-ulsan-adapter-check.json), [DB·HTTP·참조 검사 12개](busan-ulsan-import-check.json)가 통과했다. 전자는 당시 부산 메타데이터 106개·연산 166개와 울산 전체 174페이지/2,582행을 대조한 시점별 검사이며 이후 수집분까지 통과했다고 확대하지 않는다.
56. 13:51 UTC 인덱스 스냅샷은 포털 18곳·등록정보 431,544행·명세 문서별 항목 745,655개였다. 부산·울산 신규 등록 15,133행에는 아직 검증된 칼럼을 연결하지 않았으며 명세 증가분은 기존 수집기의 진행분이다. [이번 확장 기록](continuation-20260913T1355-report.json)에 실제 인덱스 생성 시각·새 검사·진행 범위를 보존했다. 이전 58개 전수 검사는 12:44 UTC 스냅샷의 별도 검사로 유지하고 이번 검사와 구분한다. 이번에는 검증용 stop 표시를 만들지 않았으며 모든 기존 수집은 계속 진행했다. 후속 작업은 새 포털/원기관 공개 명세 어댑터 확장, 부산·인천의 평문 후보를 공식 명세와 대조, 농식품 파일 헤더, 기상청 참고 문서와 주소 DB 역사 목록 등을 계속한다.

57. 이번 최종 실행 확인은 [워커 상태](workers-20260913T1358-check.json)에 저장했다. FILE·KOSIS·공공데이터 API·부산·인덱서가 각각 한 프로세스이며 오류 로그는 0바이트였다. supervisor PID 5872의 명령행도 대조했고 stop 표시는 없다. PID는 다음 실행 때 다시 확인한다.


58. 14:29 UTC 후속 실행에서 충남 올담·전북 빅데이터 허브 공개 데이터셋 카탈로그를 확장했다. `collect_provincial.py`가 공통 파서를 제공하고, `collect_chungnam.py`·`collect_jeonbuk.py`는 supervisor가 스크립트 경로로 구분할 수 있는 별도 진입점이다. 충남 413페이지·4,124개, 전북 224페이지·2,233개 고유 등록 키가 원문 총계와 일치했다. 합계 6,357개 추가이며 각각 공공데이터 목록의 현재 스냅샷이다. 다른 통계·맞춤형·별도 카탈로그까지 완료한 것은 아니다. 전북 웹 검색 캐시의 2,225건과 실시간 수집 원문의 2,233건은 조회 시점이 다른 수치이며 원문 영수증을 기준으로 삼는다.
59. 충남 목록에는 자체 `apiIdx` 1,451개와 연계·인허가 `publicdatapk` 2,673개가 섞여 있다. 처음에는 apiIdx만 확인해 일부 페이지가 실패했고, 다음에는 숫자 외 `PUB...` ID가 있어 18페이지가 남았다. 초기 파서 이슈 보고서 1·2를 보존했고 이미 받은 413개 원문을 다시 해석하여 누락 없이 대조했다. apiIdx 키는 기존 숫자를 유지하고 publicdatapk는 프로젝트 키에 `publicdatapk-` 접두사를 넣어 충돌을 방지한다. 원문 파라미터명과 원래 ID는 별도 속성에 남긴다. FILE 3,629, API 212, SHEET 198, LINK 39, 유형 공란 46개를 임의 분류하지 않았다.
60. 충남 자체 자료의 `new tui.Grid`에 연결된 `columns.push({header, name})` 정적 선언을 화면 칼럼 명세로 수집한다. inline script 번호·원문 문자 범위·리터럴·역할 displayed_sheet_column을 저장한다. 주석은 위치를 보존한 채 제외하고, 조건문·동적 계산이 섞이면 명세로 승격하지 않는다. `dataArray` 관측값의 키로 칼럼을 추측하지 않는다. apiIdx=3199에는 연도(yr), 시군명(sigun), 성별(sx), 선정 인원수(nope)가 명시돼 있다. 화면 표의 명세이며 모든 XLSX/CSV 파일 버전이 같다는 뜻이 아니다. API 안내 Vue 템플릿과 입력변수는 출력 명세로 세지 않는다.
61. 전북 목록 유형은 FILE 2,047, API 94, LINK 85, 원문 FIlE/LINK 7개다. 상세 제목과 pId가 들어 있는 명시적 공공데이터포털 링크를 대조하며, `div#info_wrap > h4[id]`의 uddi 버전 ID·제목·버전별 표 위치·링크를 보존한다. 한 등록 아래 복수 버전이 실제 존재하며 이를 한 파일로 합치지 않는다. 설명문 변수 나열은 아직 명세가 아니다. 사이트 메뉴·footer 링크는 데이터셋 참조 큐에서 제외한다. `build_frontier.py`는 충남·전북의 자료 영역에서 관측한 링크만 referencesExternalPage로 추가하며 동일 자료나 조인 관계를 승인하지 않는다.
62. [충남·전북 원문 검사](provincial-adapter-check.json) 14개가 14:49:52 UTC에 통과했다. 원문 영수증 831개, 목록 전체 637페이지·6,357행, 당시 상세 문서 194개·화면 칼럼 4개·버전 출현 241개를 검사했다. 복수 버전 등록 25개를 확인했으며, 잘못된 페이지/자료 ID·주석/동적 칼럼 거부 검사도 포함한다. [DB·실제 HTTP 검색 검사](provincial-import-check.json) 11개도 통과했다. 출처 위치는 HTTP 필드의 raw_definition.locator에 있다. 이후 수집된 상세·칼럼이 자동으로 이 시점 검사를 통과했다고 확대하지 않는다. 58개 전체 검사는 이전 12:44 UTC 고정 스냅샷 범위를 유지한다.
63. 충남 연계 자료 상세의 selectOperation은 공개 읽기용 POST `/dataSet/entityDataAll.do`를 사용한다. publicdatadetailpk=uddi:ca11c87e-c5f1-4f27-8db6-a206ae96ff5b, oprtinnm=충청남도 보령시 관광지유입인구및총매출액조회로 메타데이터 23행을 확보했다. 요청 1행·응답 22행이며 모든 행의 부모 ID와 기능명이 일치했다. [발견 기록](chungnam-operation-metadata-discovery.json)에 보존했으나 아직 출력 칼럼 DB에 넣지 않았다. header/list 등 구조·제어 요소가 포함되므로 역할을 구분하고 전체 연산/버전 목록을 순회하는 어댑터를 다음에 확장한다. DataList.do 관측값 호출·내장 예시 인증값·세션 경로를 사용하지 않는다.
64. 공공데이터포털 API 12,053개 첫 큐는 14:49:24 UTC에 소진됐다. 명세 관측 7,023개, 명세 경로 미해결 4,814개, 조회 실패 216개이며 명세 항목 출현 226,811개다. 큐 소진을 전체 명세 완료로 보지 않고 5,030개 미해결 경로·공개 파일/원기관 명세·별도 연산/중첩 문제를 후속 대상으로 둔다. supervisor는 완료된 첫 큐를 무한 반복하지 않는다. FILE·KOSIS·부산은 계속 실행 중이다.
65. 14:51:51 UTC 검색 인덱스는 포털 20곳·등록정보 437,901행·명세 문서별 항목 838,621개·기관별 반복을 포함한 출현 1,087,483개다. 아직 후속 정의 파일 수와 시점이 다를 수 있다. [이번 확장 기록](continuation-20260913T1455-report.json)에 시점별 큐/검사/집계를 저장했다. 검색 UI 이름·진행 상태·포털 등록부·model.json 카탈로그 파일 연결도 확장했다. 현재 국내 전체 수집, 모든 칼럼, 의미 관계 승인, 통계적 상관분석은 완료가 아니다.
66. [14:55 실행 확인](workers-20260913T1455-check.json)에서 FILE 26304, KOSIS 25268, 부산 16492, 충남 26800, 전북 9856, 인덱서 32956이 각각 한 프로세스이고 현재 오류 로그는 0바이트였다. supervisor는 24628, 검색 서버는 24556이며 stop 표시는 없다. 새 스크립트 허용 목록 반영을 위해 supervisor만 이전 5872의 명령행 확인 후 재시작했다. 새 목록의 중간 파서 실패는 원문을 재사용해 고쳤으며 이전 실패 로그를 삭제하지 않았다. 다음 실행에는 현재 PID와 명령행을 다시 대조한다. Git 변경 공개나 다른 프로젝트 수정 없이 이 프로젝트 수집을 유지했다.


67. 15:28 UTC 후속 실행에서 기존 FILE·KOSIS·부산·충남·인덱서가 각각 정상 실행 중임을 명령행으로 확인했다. 전북 2,233개 기본 상세 큐는 15:24:45 UTC에 소진되었고 전부 공개 메타데이터/원문 버전 정보를 확보했으나 칼럼 명세는 미확인이다. supervisor는 이 완료 큐를 다시 시작하지 않는다. 전북 각 파일/API 버전의 원기관 명세와 다른 포털 목록은 여전히 후속 범위다.
68. `collect_chungnam_operations.py`를 추가해 충남 API 212개 중 연계 publicdatapk 211개를 대상으로 `.tabs_opnm li`의 공식 selfCall 탐색을 순회했다. 자체 apiIdx API 1개는 별도 경로로 남겼다. 기본 자료의 명시적 publicdatadetailpk/oprtinnm/oprtinseqno를 정적 리터럴로 읽고, 다른 탭은 공개 GET form의 sheetorder를 변경한 문서만 조회했다. 인증값·관측 API·로그/추천 쓰기 요청은 사용하지 않았다. `/dataSet/entityDataAll.do`의 각 응답 행이 요청한 자료 버전 ID·기능명과 일치하는지 확인한다. API 211개 중 198개는 명세가 확인됐고, 11개는 기능 탭이 없으며, 2개는 파라미터 조회가 빈 목록이다. 후자 13개는 완료 명세로 집계하지 않는다.
69. 충남에서 보이는 기능 탭 254개가 자료 버전 ID·기능명 기준 212개 명세 요청으로 연결됐다. 반복 42개를 탭 출현 배열에 남기고 실제 출력 칼럼을 중복 합산하지 않았다. 청양 통계 publicdatapk-15001170은 16개 탭이 4개 기능(토지·자동차·사업체·인구)을 각각 4회 보여 준다. 서로 같은 번호/제목이라고 미리 합친 것이 아니라 각 탭 문서를 실제 조회해 버전 ID까지 대조했다. 출력 명세 행 3,690개 중 항목 3,246개를 인덱싱하고 header/body/items/item/list/response/result 같은 일반 요소명 444개는 구조·스칼라 여부를 단정하지 않고 별도 보존했다. 요청 변수 851개는 출력 수에 더하지 않는다. 이 항목 수도 고유 통계분석 변수 수나 검증된 의미 관계 수가 아니다.
70. publicdatapk-15096050은 JS 기능명 끝에 공백이 있어 초기 파서가 화면 라벨과 다르다고 거부했다. HTML에 표시되는 이름의 공백만 비교용으로 정규화하고 실제 조회 인자의 뒤 공백은 그대로 유지해 명세 11항목을 복구했다. 이전 오류를 prior_parse_outcomes에 보존했다. 최초 실행 큐 보고서와 첫 60개 문서 QA를 별도 initial 보고서로 보존하고 최종 저장 문서 기준 198/11/2 및 3,246항목으로 재대조했다. 수집 성공 요청을 반복하지 않았다. 이 211개 유한 큐는 종료되었으며 supervisor에 반복 워커로 등록하지 않았다.
71. 새 제공처 SGIS 통계지리정보서비스의 현재 공식 루트 sgis.mods.go.kr에서 /view/index → 개발지원센터 home.html → DATA API data.html 공개 링크를 확인했다. `collect_sgis.py`가 문서 본문의 `.apiItem` 68개, 고유 앵커 68개와 요청·응답 표 68쌍을 수집했다. 등록 키 data-guide-<앵커>는 문서 이름공간의 ID이며 예제 JSON의 API_... 값을 공식 식별자로 추측하지 않는다. 응답 표 620행 중 출력 명세 618항목, JavaScript 산출물 설명 1행, 3셀이어야 하나 2셀인 미해결 행 1개를 구분한다. 입력 244행은 출력 수에 합산하지 않는다. API 인증·관측값 호출을 하지 않았으며 원문에 적힌 예전 sgisapi.kostat.go.kr 주소와 연도 표기는 운영/최신성이 검증된 주소로 변경하지 않는다.
72. SGIS 메뉴의 숫자 앵커는 76개이나 본문 68개와 대조했을 때 #107~#114 8개가 없다. #106은 메뉴의 ‘도시권 목록’과 본문의 ‘소지역 코드찾기’가 다르며, 나머지 제목 차이도 원문 그대로 보존했다. 이름이 같다는 이유로 #114를 #106에 자동 연결하지 않는다. 층별 사업체 공간 속성의 type 행은 값·설명 경계가 없어 미해결로 남겼다. 총조사 주요지표 naesuoga_ppltn의 값 열에 설명 문장이 들어 있는 등 원문 이상도 보존하고 datatype을 임의 생성하지 않는다. [SGIS 코드표 후속 큐](inventory/sgis-code-table-frontier.json)에 팝업 참조 35개·고유 코드표 이름 14개를 저장했으며 아직 대상 코드표를 읽지 않았다. 지도 API·SDK·자료제공 파일 사전과 본문 없는 메뉴를 다음에 조사한다.
73. [충남 전체 기능 원문 검사](chungnam-operations-source-check.json) 10개는 211개 문서·212개 기능·3,246항목 및 출처 영수증 478개를 대조했다. [SGIS 원문 검사](sgis-source-check.json) 10개는 68개 본문 전체·618항목·입력/예외 행·메뉴 차이를 확인했다. [DB·실제 HTTP 검색 검사](sgis-chungnam-import-check.json) 11개도 통과했다. SGIS 입력 인증 변수를 응답으로 검색하지 않는지, JavaScript 산출물이 칼럼 수에 들어가지 않는지, 충남 반복 탭이 16개여도 실제 항목 16개만 인덱싱되는지 검사했다. 이 단계는 신규 자료의 검사이며 이전 58개 전체 검사는 여전히 12:44 UTC 고정 스냅샷 범위다.
74. 15:42:33 UTC 인덱스는 포털 21곳·등록정보 437,969행·명세 문서별 항목 894,188개·기관별 반복을 포함한 출현 1,143,050개다. 새 SGIS 목록과 충남 기능별 명세를 검색 UI·진행 표·제공처 등록부·README에 연결했고 model.json의 파일 목록/수량은 증분 인덱서가 반영했다. [이번 확장 기록](continuation-20260913T1546-report.json)에 실제 검사·큐·인덱스 생성 시간을 보존했다. 전국 포털 명부, 모든 칼럼, 의미 동일성·조인·상관관계 승인은 계속 미완료다.
75. [15:47 워커 확인](workers-20260913T1546-check.json)에서 FILE 26304, KOSIS 25268, 부산 16492, 충남 26800, 인덱서 32956이 각 하나이며 현재 오류 로그는 0바이트였다. supervisor 24628과 검색 서버 24556도 명령행으로 확인했고 stop 표시가 없다. 이번에는 supervisor나 기존 수집기를 중단/재시작하지 않았다. 종료된 충남 기능 큐·SGIS 단일 문서 큐·전북 상세 큐를 완료로 반복 조회하지 않는다. 다음 실행에는 실제 PID와 명령행을 다시 확인하고, SGIS 코드표/다른 공개 명세와 남은 국내 제공처로 계속 확장한다. Git 작업이나 다른 프로젝트 수정은 하지 않았다.


76. 16:18 UTC 후속 실행에서 FILE·KOSIS·부산·인덱서가 정상 실행 중이며 중복 프로세스가 없음을 확인했다. 충남 기본 상세 큐 4,124개는 16:10:23 UTC에 소진되었고 자체 화면 명세 447개 자료·5,848항목, 나머지 3,677개는 기본 화면 명세 미확인이다. 별도 연계 API 3,246항목과 합쳐 충남의 명세 문서별 항목은 9,094개지만 모든 자료·파일 버전의 칼럼을 완성한 것은 아니다. supervisor는 종료된 기본 큐를 다시 시작하지 않는다.
77. `collect_sgis_codes.py`가 이전 SGIS DATA API 문서의 popup(filename) 함수와 연결된 공개 dataCode/<filename>.html 14개를 읽었다. 코드표 461행, API 인자 참조 35개를 보존했다. ThemeCode/ThemeCode2의 대분류 병합 셀은 실제 rowspan 범위로만 확장하고 각 논리 셀에 원문 행·셀 위치를 남긴다. CoordCode의 쉼표로 나열된 EPSG 코드를 임의 분할하지 않고, 연령 코드의 선행 0·겹치는 연령 구간, 건축연도 코드의 기준년도 165행을 그대로 보존한다. 같은 코드 문자열이 다른 기준연도에서 반복된다고 합치지 않는다. 이 461행은 코드 목록 행이며 명세 칼럼 수에 더하지 않는다.
78. `model.json`에 CodeList와 referencesCodeList를 추가하고 코드표 및 참조 파일을 연결했다. 각 참조는 sgis-data-guide-<앵커> 등록 ID, 요청/응답 구분, 원문 인자 라벨·표/행/onclick 위치, 코드표 ID, 양쪽 근거를 가진다. 이는 문서에 표시된 참조 관측이며 다른 포털의 코드 체계와 같거나 자료를 조인할 수 있다는 승인이 아니다. SGIS 기존 68개 명세 문서에도 참조를 추가했지만 출력 항목 618개는 그대로다. 원래 SGIS 수집기도 같은 원문 해시이면 추가 코드표 참조를 보존하도록 수정했다. 다른 SGIS 코드 목록·지도 API·SDK·파일 사전은 남은 범위다.
79. 경남 빅데이터 허브의 현재 공식 등록 URL을 조회했으나 Python은 인증서 체인 검증 실패, 별도 웹 조회는 시간 초과였다. [접근 QA](gyeongnam-access-qa.json)에 기록했고 TLS 검증을 끄거나 폐기된 경로를 추측해 접속하지 않았다. 다른 제공처로 진행했으며 다음에는 정상 공식 HTTPS 경로·인증서 체인을 확인한다. 이 실패를 포털 폐쇄·인증 필요·수집 완료로 분류하지 않는다.
80. 새 `collect_jeju.py`가 제주데이터허브의 공개 앱 JS에 명시된 GET /api/data와 /api/data/count 읽기 요청 및 화면 기본 검색 인자를 사용한다. 익명 데이터 목록 126페이지·고유 등록 ID 1,259개가 페이지별 recordsTotal/recordsFiltered와 별도 count 1,259에 모두 일치했다. 원문 응답에는 파일 버전과 API 파라미터 정의도 포함돼 있어 /api/data/view의 조회수 증가나 관측 API 요청 없이 수집했다. FILE 649개, LINK 516개, API 73개, API+FILE 20개, FILE+LINK 1개로 유형 조합을 보존한다. 기본 authYn=false 조회에도 authYn=true인 등록 926개와 false 333개가 포함되므로 이 조회 인자를 인증 없는 원본만을 모은 필터로 오해하지 않는다.
81. 제주 API 93개에서 원문 type=request/response와 apiId를 대조하여 출력 686항목·입력 331항목을 분리했다. 각 항목의 근거는 목록 응답 data[n].dataApi.dataApiElements[m]에 있다. paramType이 없는 경우 예시 값으로 자료형/단위를 추측하지 않았다. 파일 및 미리보기 메타데이터 출현 2,240개는 dataset_file/dataset_preview/api_preview 역할과 파일 묶음 ID·세부 파일 ID를 구분한다. 이 숫자는 중복 제거한 파일 수나 확보한 파일 헤더 수가 아니다. API 주소에 내부 IP가 적혀 있어도 실제 해당 주소로 접속하지 않는다. 원본 파일·관측값 API·신청·로그인·할당량 변경 요청은 실행하지 않았다.
82. 제주 포털에는 민간/개인 업로드와 해외 주제도 있다. source owner는 화면에서 ‘소유자’로 표시되므로 이를 공공기관 생산/제공 주체로 단정하지 않고 source_owner_as_reported와 업로더 이름을 별도 보존했다. 제공기관 코드를 업로더 계정 ID로 만들어 넣지 않는다. 인포그래픽·보고서·센터 데이터 목록 및 원본 파일 명세는 남은 범위다. 명시적 dataLink URL 517개는 외부 참조 큐에 연결했으며 API endpoint 문자열은 후속 크롤링 목표로 만들지 않았다. [참조 대조](jeju-external-reference-check.json)에서 원문 URL 517개가 일치하고 의미 동일성/결합 관계 미승인 상태임을 확인했다.
83. [SGIS 코드표·제주 원문 검사](sgis-codes-jeju-source-check.json) 12개가 통과했고 원문 영수증 142개, SGIS 코드표 14개·461행·참조 35개, 제주 전체 목록 1,259개·출력 686개·입력 331개·파일 정보 출현 2,240개를 대조했다. [DB·실제 HTTP 검색](sgis-codes-jeju-import-check.json) 12개 및 제주 외부 참조 1개 검사도 통과했다. SGIS 코드 행이 칼럼 수를 늘리지 않는지, 제주 파일 자료에 API 칼럼을 잘못 붙이지 않는지, 모델의 코드표 관계가 실재 등록 ID로 해석되는지도 검사했다. 이전 58개 전수 검사는 12:44 UTC 고정 스냅샷 범위를 유지하며 이번 범위를 그 전체 검사로 확대하지 않는다.
84. 16:33:29 UTC 인덱스는 포털 22곳·등록정보 439,228행·명세 문서별 항목 934,638개·기관별 반복을 포함한 출현 1,183,500개다. [이번 확장 기록](continuation-20260913T1635-report.json)에 실제 시점별 수량·큐·검사를 저장했다. 검색 UI·README·제공처 등록부·model.json의 파일 참조도 확장했고 전체 국내 포털/모든 칼럼은 아직 미완료다. 제주 기본 목록 큐와 SGIS 참조 코드표 큐는 종료된 유한 작업이며 supervisor 반복 워커로 등록하지 않았다.
85. [16:34 워커 확인](workers-20260913T1635-check.json)에서 FILE 26304, KOSIS 25268, 부산 16492, 인덱서 32956이 각각 하나이고 현재 오류 로그는 0바이트였다. supervisor 24628 및 검색 서버 24556도 명령행으로 확인했고 stop 표시는 없다. 기존 수집기를 중단/재시작하지 않았다. 다음 실행에 PID·명령행·큐 소진 상태를 다시 확인하고, 제주 다른 목록/파일 헤더·SGIS 다른 공개 명세와 미수집 국내 제공처로 확장한다. Git 작업·다른 프로젝트 수정·메시지 발송은 하지 않았다.


86. 2026.09.13 17:06 UTC 후속 실행에서 기존 FILE·KOSIS·부산·인덱서의 실제 명령행과 정상 진행을 확인했다. 충남·전북 등 끝난 유한 큐를 반복 시작하지 않고 새 제공처 HRDK와 기존 농식품 파일 명세의 빈 부분으로 확장했다.
87. `collect_hrdk.py`가 한국산업인력공단 공식 /main#section2의 API 카드 69개와 고유 외부 참조 69개를 수집했다. 페이지의 제목·설명·소관 시스템 라벨·JSON/XML 표기를 원문 위치와 함께 보존했다. `data-go-link-<publicDataPk>`는 실제 href에서 만든 프로젝트 등록 키이며 HRDK 고유 데이터셋 ID라고 주장하지 않는다. 소관 시스템을 기관 코드로 만들지 않으며 이 69개를 안내문의 개방 API 229개 전체라고 간주하지 않는다.
88. [참조 키 대조](inventory/hrdk-api-reference-resolution.json)에서 59개가 기존 data.go API 등록 키와 일치하고 그중 57개에는 이미 수집한 응답 명세가 있었다. 해당 칼럼을 HRDK 등록정보에 다시 복사해 수량을 늘리지 않았다. 나머지 10개 실제 공식 링크는 모두 HTTP 404였으며 [실패 근거](hrdk-unresolved-target-check.json)를 보존했다. 59개는 저장된 목록과의 대조이지 해당 시점 실제 URL 정상 동작을 재확인한 결과가 아니다. 69개 전체를 외부 참조 큐에 연결했고 의미 동일성·조인 가능성을 승인하지 않았다. HRDK 상세 정의에 target_reference_qa를 추가했으며 다음 인덱스 주기에 검색 상세에 반영된다.
89. HRDK 공식 MCP 안내에 게시된 통합 /mcp와 분야별 6개 경로에서 tools/list 메타데이터만 조회했다. 최초 Accept */* 요청은 HTTP 406이었고 영수증을 남긴 뒤 공식 Streamable HTTP 규격의 application/json, text/event-stream으로 재조회하여 성공했다. common.fetch의 선택적 accept 인자로 요청 헤더를 근거에 기록하며 기존 수집기의 기본값은 그대로다. 받은 SSE의 JSON-RPC 응답 ID와 tools 배열 위치를 대조했다. 통합 경로는 안내 41개와 실제 45개, NCS는 안내 5개와 실제 6개로 다르다. 7개 경로의 계약 출현 87개, 입력 속성 출현 223개, 응답 스키마 0개다. 같은 이름이라도 경로별 이름공간을 유지하고 API 등록 69개와의 매핑은 추측하지 않는다. MCP 도구 호출·실제 데이터 요청·사용자 계정 연결·플러그인 설치는 하지 않았다.
90. `model.json`에 PublicToolContract와 advertisesPublicToolContract를 추가하고 [도구 계약](inventory/hrdk-mcp-tool-contracts.json)·[출처 관계](inventory/hrdk-mcp-tool-relations.json)를 별도 연결했다. 계약의 설명·annotations는 신뢰되지 않은 출처 텍스트로 보존하고 행동 지시나 안전성 인증으로 해석하지 않는다. 도구 입력 속성을 데이터셋 응답 칼럼이나 통계분석 변수 수에 합산하지 않는다. 현재 새 HRDK 등록 69개 자체의 정식 칼럼 수는 0이며 실제 자료 명세는 외부 참조로 찾아가야 한다.
91. `collect_mafra_previews.py`가 기존 농식품 FILE 등록 619개 상세 화면 전체를 대조했다. 459개 자료에 미리보기 버튼이 있었고 1,516개 버튼 출현을 (data_id, file_ty_code, file_sn) 기준 1,510개 요청으로 연결했다. 반복 버튼 6개를 위치 배열에 보존하고 중복 요청하지 않는다. [대상 파일 버전](inventory/mafra-preview-targets.json)은 수집된 상세 화면에 노출된 범위이며 모든 파일 버전의 전수 명부가 아니다. 파일 다운로드 링크에서 미리보기 경로를 임의 생성하지 않고 실제 버튼과 공식 JS callback 및 POST 기본값을 확인했다.
92. 공개 previewOpenDataWebFile 응답은 요청당 2 MB로 제한하고 원본 파일 전체는 다운로드하지 않는다. 원문 listHead를 공식 JS가 thead로 렌더링하는 셀만 머리글 후보로 저장하며 listRow의 관측값을 칼럼으로 추출하지 않는다. 원문 영수증에는 공개 미리보기의 표본 행이 포함될 수 있다. 파일 버전·셀 위치·각 응답 evidence_id를 preview_headers에 보존하고 fields=[]를 유지한다. 제목·빈칸·다중 머리글이 있을 수 있어 정식 명세로 승격하지 않는다. 자료에 미리보기 버튼이 없으면 기존 상세 문서를 재사용하며 같은 evidence_id의 중복 문서를 만들어 기존 칼럼을 지우지 않는다.
93. 미리보기 QA에서 HTTP 200이지만 HTML ‘서비스 장애’인 6건, HTTP 500 1건을 발견해 미해결로 보존했다. numberOfCells=0/listHead=null/listRow=null 한 건은 정상 빈 미리보기이며 기존 파싱 실패 원인도 보존한 채 캐시만 재해석했다. [빈 결과 수정 검사](mafra-preview-null-empty-check.json)는 새 HTTP 요청 없이 완료했다. 수집기 파서 2판에 이 처리를 추가했고 중단 표시는 파일 버전 요청 사이에서 확인한다. 중단된 자료 문서를 완료로 저장하지 않고 이미 받은 영수증부터 재개한다.
94. 증분 인덱서의 휴대용 머리글 내보내기를 여러 파일 버전·출처 ID에 맞춰 보완했다. 기존 csv-header-candidates.jsonl.gz 파일명은 유지하되 header_candidate_kind로 CSV 첫 레코드와 공개 미리보기 머리글을 구분하고 preview_headers 출처를 포함한다. CSV 전용 합계와 전체 머리글 후보 합계를 분리했다. HRDK·새 큐를 검색 UI·README·제공처 등록부·외부 참조 큐에 연결했다. 17:21:26 UTC 인덱스는 등록정보 439,297개, 포털 23곳, 명세 문서별 항목 964,546개, 제공기관 등록 반복 포함 출현 1,213,408개이며 서로 다른 두 항목 수를 더하지 않는다.
95. [원문 검사](hrdk-mafra-source-check.json) 12개가 17:29:11 UTC에 통과했다. 영수증 1,259개 중 수신 원문 1,258개의 SHA256과 길이, 전체 HRDK 카드·도구 계약·619개 농식품 상세 버튼을 대조했다. 명시된 미리보기 스냅샷은 문서 140개·파일 버전 631개·머리글 후보 5,411셀이다. 버전별 결과는 후보 관측 615·빈 결과 9·HTTP 실패 1·파싱 미해결 6개다. 이후 수집분까지 이 검사 범위로 확대하지 않는다. 초기 검사 결과는 hrdk-mafra-source-check-initial.json에 보존했다. [실제 HTTP·DB·내보내기 검사](hrdk-mafra-import-check.json) 12개도 통과했으며 후보의 검색 노출/정식 명세 필터 제외와 참조 69개를 확인했다. 이전 58개 전체 검사는 계속 12:44 UTC 고정 스냅샷 범위다.
96. 새 큐 등록과 머리글 내보내기 코드 반영을 위해 명령행을 확인한 supervisor와 인덱서만 재시작했다. 기존 FILE 26304·KOSIS 25268·부산 16492는 계속 동작했다. 미리보기 수집기는 파서 수정 후 협력 중단·재개하여 [17:30 워커 확인](workers-20260913T1730-check.json) 당시 PID 22548, 인덱서 15780, supervisor 7296, 검색 서버 24556이다. 모든 활성 큐가 정확히 하나이고 현재 오류 로그 0바이트, stop 표시 없음이다. PID는 재사용될 수 있으므로 다음 실행에서 명령행을 다시 확인한다. supervisor 허용 목록과 workers.json에 mafra-previews가 등록되어 있으며 소진되면 재시작하지 않는다.
97. [이번 확장 기록](continuation-20260913T1730-report.json)에 시점별 수량·검사·큐 상태를 보존했다. 17:30 부근 FILE 34,800/83,691, KOSIS 36,600/266,688, 부산 6,619/12,551, 농식품 미리보기 자료 188/619를 처리 중이었다. 헤더 후보는 실제 값의 품질 점수·의미 관계 승인·상관분석 결과가 아니다. 다음 실행은 중복 워커 없이 이 큐를 이어가며 HRDK 안내와 실제 목록의 차이, 404 참조, 남은 국내 제공처·SGIS/제주 추가 공개 명세를 조사한다. 전국 제공처 명부와 모든 칼럼은 미완료다. Git 커밋·푸시·게시·다른 프로젝트 수정·타인 메시지 발송은 하지 않았다.


98. 2026.09.13 18:02 UTC 후속 실행에서 FILE·KOSIS·부산·인덱서가 각각 하나로 진행 중임을 확인했다. 농식품 미리보기 큐는 17:43:52 UTC에 619개 등록정보 전체를 처리하여 소진되었고 supervisor가 재시작하지 않았다. 새 범위는 식품안전나라의 공개 데이터 목록·응답 명세로 확장했다.
99. `collect_foodsafety.py`는 공식 / → /apiMain.do → 공개 datasetList.do 메뉴 및 화면 JS의 POST searchDatasetList.do를 따른다. 전체 분류·제공기관·유형을 비운 검색과 UI에 표시된 50개씩 보기를 사용해 4페이지·고유 svc_no 178개를 목록 total_cnt와 대조했다. API 169개·FILE 8개·LINK 1개다. I-0050 같은 하이픈 포함 서비스 ID를 보존하고 공공데이터포털 publicDataPk와 혼동하지 않는다. 제공기관 이름은 원문대로 저장하되 대부분 빈 기관 코드에 이름을 근거로 코드를 만들어 넣지 않는다. UI 필터의 분류·기관·유형 옵션 39개(각 전체 옵션 포함)는 별도 원문 위치와 보존하며 자료별 코드를 역추정하지 않는다.
100. 독립적인 API_TYPE06 검색 4페이지도 169개 고유 ID이고 전체 목록의 API ID와 정확히 일치했다. 그러나 이전 /api/main.do 홈에는 XML/JSON 172개, 2026.09.02 공지에는 171종이라고 표시되어 [범위·기준일 차이](foodsafety-catalog-crosscheck.json)를 미해결로 남겼다. 현재 178개 목록 스냅샷 대조 완료를 포털 전체·모든 과거 서비스의 전수 완료로 확대하지 않는다. I2791은 실제 목록 렌더러의 명시적 분기에 따라 data.go.kr/data/15127578/openapi.do로 연결하고 임의의 자체 상세 명세를 만들지 않았다.
101. 공식 상세 화면 177개를 읽어 API 169개에서 응답 항목 1,861개를 확보했다. API 페이지마다 변수 목록 표가 두 번 표시되는 169개 사례는 전체 원문 행이 정확히 일치할 때만 중복 수량을 제외하고 두 위치를 보존했다. 출력 표에는 자료형·단위가 없으므로 표본 값에서 추측하지 않는다. 입력 표의 1,315행 중 실제 API 등록 화면은 1,275행, FILE 화면의 공통 입력 템플릿은 40행이다. 후자가 파일 서비스를 API로 사용할 수 있다는 증거는 아니다. 메시지 코드표·SAMPLE 관측값을 출력 칼럼에 합산하지 않는다. 별도 관측 API·샘플 API·인증키 신청은 실행하지 않았다. 상세 HTML 자체에 포함된 공개 샘플은 원문 영수증에 남아 있다.
102. FILE 8개 화면에서 파일 버전 정보 316행과 다운로드 버튼의 실제 href/onclick을 저장했다. 크기 단위 KB는 화면 표기 그대로 보존하고 실제 바이트 수라고 고쳐 추측하지 않는다. 원본 파일을 다운로드하지 않았으며 파일 내부 칼럼은 미확인이다. API와 FILE 예시 2개의 svc_no GET 퍼머링크도 실제 응답 식별자를 확인했다. [정의 요약](foodsafety-definition-summary.json), [퍼머링크 검사](foodsafety-permalink-check.json)를 참고한다.
103. 공식 공지 1151453은 2026.08.10부터 개인정보 유·노출 우려 항목을 빈 값(Null)으로 제공하며 항목 자체 삭제는 별도 공지 예정이라고 설명한다. 공지 1151919는 2026.09.03 적용 CSV 제공·CHNG_DT·호출 제한 변경을 설명한다. 두 본문의 #bdt_pre 위치·URL·SHA256을 보존하고 API 정의에 출처 참조를 붙였다. 특정 출력 칼럼이 반드시 마스킹되는지, 실제 값의 결측률이나 현행 서비스 동작은 검증하지 않았다. 칼럼 존재와 값의 가용성을 구분한다. [사이트별 QA](foodsafety-source-qa.json)의 실제 측정은 최종수정일 기재 144/178·업데이트주기 158/178·최초개방일 159/178·호출제한 표기 177/178이다. 이는 표시 여부이며 최신성·정확도·신뢰도 점수가 아니다. [메타데이터 검사](foodsafety-metadata-presence-check.json)에서 원문 708개 셀과 위치를 대조했다.
104. [식품안전나라 원문 검사](foodsafety-source-check.json) 11개가 수신 원문 189개 해시와 전체 목록·177개 상세·반복 표·입력/출력 구분·파일 행·공지·수량 차이를 검증했다. [DB·실제 HTTP 검사](foodsafety-mafra-import-check.json) 11개도 통과했다. 검색의 목록 178개·명세 보유 169개·응답 1,861항목, keyId 입력 제외, FILE·LINK 상세 구분 및 개인정보 공지 참조를 확인했다. 나중의 수집분까지 자동으로 이 검사 범위에 포함하지 않는다.
105. 농식품 미리보기 전체 원문 대조를 다시 수행해 명세 문서 459개·파일 버전 1,510개·머리글 후보 16,990셀을 확인했다. 버전별로 후보 관측 1,471개·빈 결과 30개·HTTP 실패 3개·HTML 서비스 장애 6개다. HTTP 실패는 500 한 건과 시간 초과 두 건이며 원문 영수증을 보존했다. [최종 범위 보고서](mafra-previews-final-report.json), [원문 검사](hrdk-mafra-source-check.json), [최종 내보내기 검사](mafra-final-portable-export-check.json)를 참고한다. 전체 재검사는 12개 검사·영수증 2,138개이며 이전 140개 문서 검사는 hrdk-mafra-source-check-1730.json에 보존했다. 머리글이 있는 450개 자료·16,990셀은 압축 내보내기에도 모두 반영되었다. 원본 파일 칼럼의 의미 검증이나 모든 파일 버전 전수 완료를 뜻하지 않는다.
106. build_frontier.py가 매번 수만 개 정의 JSON 파일을 여는 대신 증분 인덱서의 schema_documents.metadata_json과 field_count를 읽도록 변경했다. 기존 포털 및 <portal>-schema- 문서 범위·URL·evidence_id·locator를 유지한다. 이제 참조 목록은 검색 DB에 반영된 스냅샷 범위이므로 새 정의를 인덱싱한 뒤 실행해야 한다. [변경 대조 검사](frontier-sqlite-adapter-check.json)에서 기존 참조 53,205개 모두의 식별자·출처·위치·의미 미승인 플래그가 유지되고 새 53,776개 목록에 누락 0개임을 확인했다. 새 DB 방식 실행 시간은 3.584초다. 이 검사는 속도뿐 아니라 기존 참조 손실 여부를 확인한 것이다.
107. 변경 적용 시 명령행을 확인한 인덱서 15780만 중지하고 indexer.stop으로 중복 재시작을 막은 뒤, 새 코드로 한 번 증분 반영·내보내기·참조 대조를 수행했다. 기존 FILE·KOSIS·부산 수집기는 계속 실행되었다. stop 표시를 제거한 뒤 기존 supervisor가 인덱서 PID 32208을 재개했다. [18:22 워커 검사](workers-20260913T1822-check.json)에서 FILE 26304·KOSIS 25268·부산 16492·인덱서 32208이 각 하나, supervisor 7296·검색 서버 24556 정상, 활성 워커 오류 로그 0바이트, stop 표시 없음이다. 식품안전나라 178개와 농식품 미리보기 큐는 종료된 유한 범위이므로 다시 반복 시작하지 않는다.
108. [이번 확장 기록](continuation-20260913T1822-report.json)의 18:20:49 UTC 인덱스는 24개 포털·등록정보 439,475개·명세 문서별 항목 1,002,649개다. 통계 분류·응답 제어 변수·출처 중복을 포함하는 항목 수이며 고유 분석 변수 수가 아니다. 18:22 부근 큐는 FILE 38,700/83,691·KOSIS 39,675/266,688·부산 8,173/12,551이다. 다음에는 이 큐를 중복 없이 유지하고 아직 미수집인 국내 제공처, 식품안전나라 수량 차이·파일 명세, 농식품 미해결 미리보기의 정상 대체 경로, SGIS/제주 추가 공개 문서 등을 조사한다. 국내 전수·모든 칼럼·통계 관계 검증은 계속 미완료다. 이전 58개 전체 검사는 12:44 UTC 고정 스냅샷 범위를 유지한다. Git 작업·타인 메시지·다른 프로젝트 수정은 하지 않았다.

109. 2026.09.13 18:54 UTC에 시작한 확장에서 게임물관리위원회와 산림청을 추가했다. 작업 도중 실행 환경이 중단되었고, 2026.09.14 06:34 UTC 후속 확인에서 기존 수집기·인덱서·감시기·검색 서버가 모두 없었다. 마지막 진행 저장은 전날 19:11 UTC 부근이었으며 중단 원인은 확인되지 않았다. stop 표시는 없었다. 06:35–06:36 UTC에 검색 서버와 기존 supervise.ps1을 숨김 실행하여 저장된 원문/정의를 재사용해 FILE·KOSIS·부산·인덱서만 재개했다. 소진된 큐는 다시 시작하지 않았다. 기존 worker PID는 더 이상 유효하지 않으므로 아래 최신 상태와 실제 명령행을 함께 확인한다.
110. `collect_grac.py`는 공식 게임·채용 API 메뉴가 직접 연결하는 이용 가이드 2개를 등록하고 응답/제어 항목 24개·입력 10개·구조 요소 4개를 분리했다. 프로젝트 키 game/recruit는 공식 데이터셋 ID라고 주장하지 않는다. 요청 표의 X rowspan=5는 실제 HTML 셀 범위를 펼쳐 적용하며 각 물리 셀의 위치를 보존한다. 게임 출력 표의 `canceledddate`와 예시의 `canceleddate`, XML 선언의 잘못된 닫힘·태그 오류를 고치지 않았다. 채용 출력 표의 헤더가 요청변수라고 적혀 있어 명시적 ‘출력 변수’ 제목으로 역할을 식별하고 불일치를 남겼다. `result` 두 행은 별도 구조 행으로 유지하며 예시의 `results`·`resultItem`을 새 칼럼으로 승격하지 않는다. 오류 코드의 선행 0도 보존한다. API 서비스는 호출하지 않았다.
111. 게임물관리위원회 공공데이터 개방 안내도 읽었으나 그 표는 담당자 안내이며 데이터셋 목록/칼럼이 아니다. [안내 페이지 탐색](inventory/grac-disclosure-navigation.json)에 공공데이터포털·제공신청·Open API 링크만 기록했다. 전체 기관별 데이터 목록 대조·파일 명세는 남아 있다. [게임물관리위원회 QA](grac-source-qa.json)는 문서 내 일관성 관측이며 실제 API 응답/품질 점수가 아니다.
112. `collect_forest.py`가 data.forest.go.kr의 짧은 페이지 안에 실제로 실행되는 document.location.href 문장을 찾아 산림청 공개 안내와 개방목록으로 연결했다. 주석 처리된 옛 경로나 별도 noscript 경로를 현행 경로로 혼동하지 않는다. [탐색 근거](inventory/forest-navigation-observations.json)를 보존하고 포털을 폐쇄로 판정하지 않았다. 명시된 분류 탭 1·3·4·5·6의 카드 8·13·3·2·30개, 총 56개를 수집했다. tab=2를 추측해 요청하지 않았다. 원문 전체 건수 표시는 없으므로 이 5개 문서의 모든 카드를 읽었다는 것과 기관 전체 목록 완료를 구분한다. 원문에 있는 pblicDataId를 쓰고, ID가 없는 링크는 실제 전체 URL의 프로젝트 해시로 식별한다. 요청 URL의 익명 jsessionid만 제거하며 원문 영수증을 남긴다.
113. 산림청 자체 안내 21개 중 10개에서 API 응답 156항목·입력 50항목을 확보했다. 제공 타입이 Int인 날짜 항목도 원문 그대로 보존하고 단위는 추측하지 않는다. 원래 자체 안내에서 명세가 없는 11개를 성공 칼럼으로 세지 않았다. 외부 자료 참조 35개 중 data.go.kr 링크 9개는 기존 목록 키와 모두 일치했고 8개에는 기존 응답 명세가 있었지만, 칼럼을 산림청 등록에 복사해 중복 집계하지 않았다. [참조 대조](inventory/forest-data-go-reference-resolution.json)는 기존 저장 목록과의 키 대조이며 링크의 현행 정상 응답이나 의미 동일성을 승인한 결과가 아니다. 지도 이용 목적 제한은 원문 문구와 위치로 보존하고 재사용 허가로 추론하지 않는다.
114. 재개 후 `collect_forest_attributes.py`가 자체 안내의 실제 ‘활용정보’ 탭 19개를 따라 지도 파일/레이어 속성사전까지 확장했다. [대상 탐색](inventory/forest-usage-targets.json)의 21개 부모 안내 중 19개에 해당 링크가 있다. 맞춤형조림지도·임도망도·임상도·등산로·휴양림수목원 5개 자료, 속성 표 7개에서 칼럼 61개를 추가했다. `forest-schema-<ID>-usage.json`은 동일 등록의 별도 근거 문서이며 원래 API 문서를 덮어쓰지 않는다. 등산로의 지점/선/안전지점 표는 9·16·6항목이고 MNTN_CODE가 겹쳐도 schema_group_id별로 유지한다. 숫자 길이 `5,2`와 물음표 3개는 그대로 보존한다. EPSG 코드·길이의 숫자 해석·최신 파일 버전 적용·필드별 단위는 추정하지 않는다. 문서의 GRS80/WGS84·km/분·2016년 수정일·imsang_5th.zip 등의 문맥은 별도 보존한다. 파일·관측 API를 내려받지 않았다. [속성 요약](forest-attribute-summary.json), [산림청 QA](forest-source-qa.json)를 참고한다.
115. 산림청 최종 등록정보는 56개 그대로이며 명세를 확보한 등록은 15개, 자체 안내 6개·외부 참조 35개는 아직 칼럼 미확인이다. API 156항목과 파일 속성 61항목은 서로 다른 명세 범위다. 게임물관리위원회까지 새 등록 58개·정의 문서 77개·새 명세 항목 241개다. 의미상 고유 변수 수나 통계적 관계의 검증 수가 아니다. `build_frontier.py`에 산림청을 추가하여 고유 외부 참조 54개를 연결했다. 원문 링크 출현은 60개이며 같은 자료·URL의 추가 출처 위치는 개별 정의 문서에 남고 frontier에는 유효한 출처 한 개가 대표로 남는다. 같은 URL이 여러 문서에 있을 때 파일 순서와 DB 순서가 달라질 수 있으므로 검사는 유효한 원문 위치 중 하나와 대조한다.
116. [기본 원문 검사](grac-forest-source-check.json) 14개는 기본 등록 58개의 원문 33개 해시와 표/행/셀 위치를 대조한다. [추가 파일 속성 검사](forest-attribute-source-check.json) 8개는 활용정보 19개와 부모 안내 19개, 원문 38개 해시·속성 61개·자료별 레이어 및 실제 SQL/HTTP를 대조한다. 두 원문 수에는 공통 부모 문서가 있어 합산하지 않는다. [최종 DB·HTTP 검사](grac-forest-import-check.json) 13개가 통과했으며 파일 속성 추가 이전 13개 검사는 grac-forest-import-check-before-attributes.json에 보존했다. [휴대용 내보내기 검사](grac-forest-portable-export-check.json)는 전체 export 안의 새 241항목이 SQLite와 정확히 일치함을 확인했다. 이 검사들을 이전 58개 전수 검사나 계속 늘어나는 다른 출처 전체의 재검증으로 확대하지 않는다.
117. 재개 시 운영체제 monotonic 시간이 1시간 미만이어서 `last_export=0`으로는 인덱서 시작 시 내보내기를 하지 않는 문제를 실제로 확인했다. `last_export=float('-inf')`로 초기화해 시작 시 항상 내보내도록 수정했다. 06:41:51 UTC의 인덱스/참조 저장 완료 후 3576의 절대 명령행을 확인하여 인덱서만 재시작했다. 소유 문자열이 있는 일시적 indexer.stop을 사용한 뒤 제거했고 기존 supervisor가 PID 26444로 재개했다. FILE·KOSIS·부산은 계속 실행했다. 06:43:35 UTC의 실제 시작 내보내기가 true이며 1,294,182행 export 중 새 241항목을 대조했다. 검색 화면의 머리글 후보 설명도 CSV와 기존 공개 미리보기 모두를 포함하도록 바로잡았다.
118. [이번 확장·복구 기록](continuation-20260914T0647-report.json)의 06:43:19 UTC 인덱스는 국내 제공처 후보 124개 중 일부 목록을 확보한 26곳·등록정보 439,533개·명세 문서별 항목 1,045,320개다. 전국 모집단과 모든 칼럼은 계속 미완료다. 06:47 부근 진행은 FILE 43,200/83,691·KOSIS 43,225/266,688·부산 10,035/12,551이며 미해결을 포함한 처리 수다. [최신 워커 검사](workers-20260914T0646-check.json)에서 FILE 7712·KOSIS 15032·부산 6688·인덱서 26444가 각 하나, 현재 오류 로그 0바이트, supervisor 12668·검색 서버 2384가 정상이며 stop 표시는 없다. 다음 실행에는 실제 PID와 명령행·새 진행을 재확인한다. 게임물관리위원회/산림청/활용정보는 종료된 유한 큐로 supervisor에 반복 등록하지 않는다. 남은 국내 제공처 목록, 산림청 연결 자료의 원기관 명세·다른 파일 버전, 기존 포털 미해결 칼럼으로 계속 확장한다. Git 작업·다른 프로젝트 수정·타인 메시지 발송은 하지 않았다.


119. 2026.09.14 07:42 UTC에 시작한 후속 작업에서 이전 수집기·인덱서·감시기·검색 서버가 다시 모두 없음을 확인했다. 마지막 저장은 07:16 UTC 부근이었다. Windows 부팅 시각은 06:33 UTC로 이전 재개와 같아 이번 중단을 재부팅으로 단정하지 않는다. 원인은 미확인이다. `resume_runtime.ps1`을 추가해 실제 명령행과 stop 표시·8766 포트를 확인한 뒤 기존 설정의 미완료 큐만 숨김 실행하도록 했다. 새 자동화는 만들지 않았다. 07:45 부근 FILE·KOSIS·부산·인덱서·브라우저·감시기를 저장 지점에서 재개했다. 현재 PID는 아래 126번을 확인한다.
120. `collect_expressway.py`가 고속도로 공공데이터 포털의 빈 필터 검색 배열 527개를 수집했다. 목록 페이지의 기본값과 공개 JavaScript의 getList/getDataListCnt 경로·DataTables 클라이언트 페이지 구분을 확인했다. FILE 238·OPENAPI 93·ORG 194·LOD 2와 분야별 집계를 별도 집계 응답과 대조했고, 별도 OpenAPI 목록 93개의 apiId 집합도 일치한다. service_ID와 apiId는 다른 이름공간이며 0618 같은 선행 0을 보존한다. 원문 추천 service ID를 승인된 관계로 승격하지 않는다. [목록 대조](expressway-catalog-report.json)를 참고한다. 검색 배열을 모두 읽었다는 것과 포털의 모든 문서·파일 버전·칼럼을 확보했다는 것을 구분한다.
121. 고속도로의 초기 메타데이터 POST 일부는 잘못된 기본값/콘텐츠 협상으로 400·404·500을 반환했다. 공개 HTML의 apiCnt=0 및 JavaScript dataType=json에 맞춰 Accept: application/json을 사용한 요청은 정상 응답했다. 실패 영수증과 성공 대체 영수증은 [사이트별 QA](expressway-source-qa.json)에 함께 남겼으며 인증키·쿠키·보호된 경로 우회는 사용하지 않았다. API 93개에서 N으로 명시된 출력 1,553행·Y 입력 384행을 보존했다. 공통 key/type 입력 2행/API는 공식 렌더러의 리터럴 선언이며 JSON 응답 항목과 합산하지 않는다. 자료 884의 15행은 isInput이 공백이다. 공식 렌더러는 이를 출력 표로 보내지만 원래 구분값을 N으로 고치지 않고 unclassified_schema_rows에 행 전체·원문 위치·렌더러 근거를 보존했다. 따라서 이 15행은 정식 출력 수량에 포함하지 않는다.
122. 고속도로 HTML에서 원본 문서 56개의 명시적 ‘컬럼 설명’ 표 306행을 추가했다. FILE 237개에서 샘플 머리글 2,728셀, LOD 2개의 공개 목록 화면에서 머리글 9셀을 후보로 보존했다. LOD 화면의 번호 등 제어 항목이 실제 데이터 칼럼이라는 보장은 없다. API 출력·정식 칼럼 설명과 샘플/목록 머리글을 분리한다. FILE 1개와 원본 문서 138개는 현재 HTML에서 칼럼을 확보하지 못했다. parser_version=3으로 저장 원문을 다시 파싱했으며 새 요청으로 바꾸거나 과거 실패를 지우지 않았다. 527개 기본 큐는 소진됐으나 all_columns_complete는 false다.
123. `collect_expressway_files.py`가 원본 문서 194개의 hidden datasetId·원본 문서 표 및 공개 docu.js를 대조하고 읽기용 getList에서 파일 버전 메타데이터 806행을 수집했다. 원본 URL 링크 표가 있는 문서에서는 getDocuUrlLinkList를 읽어 외부 링크 15개를 보존했다. 모든 문서에 itemList 표가 없으므로 별도 동적 항목 호출·파일 미리보기·다운로드·활용목적 양식을 실행하지 않았다. `<key>-files.json`은 원래 HTML 칼럼과 다른 근거 문서라 API/파일 필드를 지우지 않는다. 파일별 칼럼을 실제로 읽었거나 부모 설명이 모든 버전에 적용된다고 주장하지 않는다. 이 유한 큐 194개는 08:09:55 UTC에 종료됐으며 반복 워커로 등록하지 않았다. 외부 참조 15개는 build_frontier.py와 검색 DB에 반영했다.
124. WAMIS 공개 API 홈·활용가이드를 읽었다. ApiService()는 로그인 상태를 검사하므로 보호된 API 목록이나 인증 화면을 요청하지 않았다. 강우레이더 서비스 중단·기상청 자료 연계 문제 공지 두 개를 [WAMIS 접근/공지 QA](water-source-access-qa.json)에 원문 위치와 함께 보존했다. 다른 WAMIS 공개 경로 전체가 비공개라는 뜻은 아니다. 공지가 연결한 한강홍수통제소 레퍼런스에서 `collect_hrfco.py`로 9개 연산·출력 73행·요청 표 출현 74행을 수집했다. 오류 코드 18개는 별도 표다. 24개 단위는 단위: 원문만 추출하고 m³/s·m^3/s·EL.m·El.m 같은 표기를 정규화하지 않는다. 댐/보 관측소 명칭 설명에 강수량이라고 적힌 2개 항목과 보정 전 T/M 자료 주의사항은 [HRFCO QA](hrfco-source-qa.json)에 남겼다. 이 레퍼런스 9개 연산이 WAMIS의 강우레이더 대체 서비스와 같은 범위인지는 미해결이다. HRFCO를 새 후보로 추가해 등록부는 125개 후보가 됐으며 국내 전수 명부는 아니다.
125. 새 등록 536개·정의 문서 730개에서 정식 명세 1,932항목과 머리글 후보 2,737셀을 추가했다. [원문 대조](expressway-hrfco-source-check.json) 14개 검사는 원문 843개의 SHA256·목록/집계/API ID·물리 표/행/셀·요청 rowspan·파일 버전·공지 범위를 대조한다. [DB·HTTP·내보내기 검사](expressway-hrfco-import-check.json) 13개는 730개 문서/1,932항목 일치, 149개 고속도로/9개 HRFCO 명세 등록 필터, 미분류 15행·머리글 후보·외부 링크 15개·원문 설명 불일치 보존을 확인했다. 새 명세만 [별도 압축 스냅샷](inventory/expressway-hrfco-columns-snapshot.jsonl.gz)으로 내보내 SQL과 대조했다. 전역 압축 export는 기존 인덱서의 시간별 실행이 담당하므로 현재 DB보다 늦을 수 있다. 이를 내보내려고 정상 인덱서를 재시작하지 않았다. 이전 58개 전체 검사는 여전히 2026.09.13 12:44 UTC 고정 범위이며 이번 새 자료 검사로 전국 전체가 재검증됐다고 확대하지 않는다.
126. [이번 확장·복구 기록](continuation-20260914T0817-report.json)의 08:12:52 UTC 검색 인덱스는 목록을 일부 확보한 제공처 28곳·등록정보 440,069개·명세 문서별 항목 1,091,648개다. 제공기관별 표준 연결 반복까지 포함한 SQL 명세 행은 1,340,510개로 별도 수량이다. 고유 분석 변수나 검증된 통계 관계 수가 아니다. [08:16 워커 검사](workers-20260914T0816-check.json)에서 FILE 8120·KOSIS 11416·부산 23008·인덱서 18188이 각 하나이며 활성 오류 로그 0바이트, 감시기 3744·검색 서버 3236이 정상이고 stop 표시는 없다. `check_runtime.ps1`은 같은 검사를 다시 수행할 수 있다. 고속도로 기본 큐도 소진돼 반복하지 않는다. 다음 실행에는 현재 명령행·새 진행·오류를 다시 확인하고 미수집 국내 제공처와 공개 명세를 확장한다. 국내 전수·모든 칼럼·실제 값 품질·통계적 관계 검증은 미완료다. Git 작업·다른 프로젝트 수정·타인 메시지 발송은 하지 않았다.


127. 2026.09.14 08:48 UTC 후속 수집에서 기존 FILE·KOSIS·인덱서·검색 서버·감시기가 정상임을 확인했다. 이번 단계에서는 복구나 재시작을 하지 않았다. 부산 기본 메타데이터 큐는 08:40:23 UTC에 12,551개를 마치고 자연 종료했으므로 재실행하지 않았다. 과거 큐 보고서는 최초 상세 실패 4개와 부분 파일 조회 실패를 보존하는 시점별 기록이다.
128. `retry_busan_connections.py`가 실제 연결 끊김 9개만 이전 원문/정의 문서를 보존한 채 한 번씩 재조회했다. selectDataSet 4건·selectFileData 5건 모두 성공했고 새 evidence ID에 connection-retry-20260914를 붙였다. 기존 HTTP 오류·인증·호출제한을 재시도하거나 보호된 경로로 우회하지 않았다. [재조회 기록](busan-connection-retry-report.json), [이전 정의 문서](inventory/busan-retry-original-documents.json)를 보존한다. collect_busan.collect_one의 선택적 evidence_replacements/force는 이 9건 재해석용이며 기본 큐 동작은 그대로다.
129. [부산 전체 원문 대조](busan-final-metadata-source-check.json)가 12,551개 등록 및 영수증·SHA256 25,022개를 확인했다. 공개 API 메타데이터가 있는 등록 350개·연산 442개, 파일 목록이 있는 등록 12,119개·파일 메타데이터 17,375행이다. 응답 이름 후보 7,071개와 설명 후보 7,071개는 별도 표현이므로 합산하지 않는다. 원문 링크 출현 24,295개 중 호스트 없는 http:// 자리표시자 6개는 보존하되 frontier에서 제외한다. fileCnt가 모두 문자열 0인 데 비해 fileList가 비어 있지 않은 12,119개 사례는 필드 의미가 미확인이다. 실제 파일 누락·품질 불량으로 판정하지 않는다. [부산 QA](busan-source-qa.json)와 [재조회 후 진행](busan-final-metadata-collection-report.json)을 추가했고 검색 화면에는 후자의 현재 상태를 표시한다. 원래 busan-collection-report.json은 삭제하지 않았으며 정식 칼럼은 여전히 0개다. HTML 상세 오류·다른 부산 카탈로그·원기관 명세는 남아 있다.
130. `collect_kdi_kocca.py`가 KDI 공개 Open API 안내의 A–F 분류 6개·정식 응답 및 제어 항목 113개·입력 30개를 확보했다. ARCHIVES 구조 행 6개는 칼럼에서 분리했다. cd 기본값 A가 모든 분류에 반복되는 5개 차이를 그대로 남겼으며 실제 API를 호출하거나 기본값을 수정하지 않았다. 연구자료 개별 목록 6개라는 뜻이 아니다. 공공데이터 개방 안내는 keyword 검색 링크이며 별도 자료 목록으로 세지 않았다. [KDI QA](kdi-api-source-qa.json)를 참고한다.
131. 같은 수집기가 한국콘텐츠진흥원 활용가이드 6개·응답 101개·입력 33개를 확보했다. 응답 상단 title과 List.title을 경로별로 구분하고 List.cata를 원문 그대로 유지한다. 입력 표 6개에서 tr 없는 numOfRows 셀을 실제 tbody.direct_cells 위치와 함께 보존했다. 정기간행물 출력 표의 rowspan=9는 실제 남은 7행에만 적용하고 원래 범위를 감사 정보에 남겼다. resultMgs/resultMsg, List/list, startDt/viewStartDt 등 문서·예시 불일치를 자동 보정하지 않는다. 구조 6개·오류 코드 36행은 응답 수량과 분리한다. 소개 메뉴와 실제 가이드의 명칭 차이도 [KOCCA QA](kocca-api-source-qa.json)에 있다. API·인증 신청은 실행하지 않았다.
132. `collect_kspo_referred.py`는 국민체육진흥공단 공식 공개 안내가 실제 연결한 외부 GitHub Pages 데이터 지도에서 활성 등록 183개를 확보했다. 공개 안내 표 20행·6행은 묶음이며 개별 자료로 합산하지 않는다. 외부 호스트 소유권은 미확인이다. HTML→명시된 app.js→명시된 explorer-data.json을 읽었고 코드는 실행하지 않았다. 배포 자산에 들어 있는 표본 관측 행은 raw 영수증에 부수적으로 포함되지만 관측값을 추출·검색·분석하거나 원본 CSV를 요청하지 않았다. 활성 목록에 없는 분리 payload 키 33·40·103은 등록으로 추가하지 않았다. 복사된 명세 그룹 178개와 미리보기 머리글 3,035셀은 후보이며 정식 칼럼 0개다. 앱의 예정된 관계 시나리오·basis 등급은 승인 관계·품질 점수로 가져오지 않았다. [KSPO QA](kspo-source-qa.json), [data.go.kr 참조 86개 대조](inventory/kspo-data-go-reference-resolution.json)를 확인한다.
133. `collect_culture_market_references.py`는 활성 지도에 실제로 연결된 문화 빅데이터 플랫폼 원문 92개를 별도 제공처 등록으로 수집했다. hidden id·제목·기관 링크 및 화면의 컬럼정의서 시트/호출 코드를 대조하고 공개 읽기용 columninfo.do의 type=preview/id 요청으로 칼럼 1,577개를 확보했다. 원문 여덟 셀의 순번·영문명·한글명·자료형·길이·PK·NOT NULL·상품명을 보존한다. 비연속 순번이나 단위를 임의 완성하지 않는다. 관측 미리보기·파일 다운로드·구매는 실행하지 않았다. 전체 문화 플랫폼 목록이 아니라 92개 참조 부분집합이다. [수집 상태](culture-market-references-collection-report.json), [원문 QA](culture-market-reference-source-qa.json)를 확인한다.
134. [복사된 정의서와 원문 셀 비교](inventory/kspo-culture-primary-definition-comparison.json)는 87개 완전 일치·5개 차이를 확인했다. 4개는 복사본의 자료형 등 속성 생략, 1개는 설명 표기 차이다. 이와 별개로 [미리보기 머리글과 원문 정의서 이름·순서 비교](inventory/kspo-culture-preview-dictionary-comparison.json)는 90개 일치·2개 차이다. 경륜 등록 선수 미리보기 31개/정의서 28개, 다른 자료의 WRHSN_DE 차이를 보존한다. 문서 비교와 파일 버전 동등성·사람이 승인한 의미 관계·통계 관계 검증은 구분한다. KSPO 자료에 원본 칼럼을 복제하지 않았고 외부 참조 178개에는 same_dataset_asserted/joinability_asserted=false를 유지했다.
135. [기관 원문 대조](institutional-source-check.json) 11개가 원문 SHA256 193개 및 신규 287개 등록·정식 항목 1,791개·머리글 후보 3,035개를 대조했다. [DB·실제 HTTP·내보내기 검사](institutional-import-check.json) 최종 18개가 문서/필드 전체 일치, 정식 명세 필터, 구조/오탈자/깨진 표 위치, 부산 재조회 근거, 외부 참조 178개와 현재 진행 표시를 확인했다. 새 명세 1,791행과 후보를 담은 180개 문서 행을 별도 gzip 스냅샷으로 저장하고 다시 읽어 대조했다. 후보 항목 수 3,035와 후보 문서 행 수 180을 혼동하지 않는다. 전역 압축 export는 기존 인덱서의 시간별 실행이 관리하므로 현재 SQLite보다 늦을 수 있다. 이전 전체 58개 검사는 여전히 2026.09.13 12:44 UTC 고정 범위다.
136. KOSIS의 새로운 parse_unresolved 1건(134/DT_13403)은 HTTP 200 원문에 통계표 정보가 없다는 alert가 있고 g_jsonStatInfo가 없는 사례다. [출처 오류 관측](kosis-source-error-20260914.json)에 원문 문자 위치·SHA256을 보존했다. 영구 삭제나 파서 결함으로 단정하지 않았고 자동 재조회/정식 칼럼 생성 없이 기존 실패 문서를 유지한다. 파일 텍스트의 parse_unresolved 검색은 previous_parser_attempt에도 걸리므로 현재 상태 확인에는 schema_documents.status 또는 최상위 status를 사용한다.
137. [이번 확장 기록](continuation-20260914T0920-report.json)의 09:18:37 UTC 스냅샷은 목록을 일부 확보한 제공처 32곳·등록정보 440,356개·명세 문서별 항목 1,143,674개다. 표준 명세의 제공기관별 연결 반복을 포함한 SQL 행은 1,392,536개로 별도 수량이다. 고유 분석 변수 수나 통계 관계 수가 아니다. [09:20 워커 검사](workers-20260914T0920-check.json)에서 FILE 8120·KOSIS 11416·인덱서 18188이 각각 한 프로세스이며 활성 오류 로그 0바이트, 감시기 3744·검색 서버 3236 정상, stop 표시가 없다. 이번 새 유한 수집기·검사기는 모두 자연 종료했으며 반복 워커로 등록하지 않았다. 다음 실행에는 실제 명령행과 새 진행을 다시 확인한다. 국내 후보 125개는 전수 명부가 아니며 나머지 포털·칼럼·파일 버전·재사용 조건·관계 검증을 계속한다. Git 작업·다른 프로젝트 수정·타인 메시지 발송은 하지 않았다.


138. 2026.09.14 사용자 요청에 따라 기존 수집 메타데이터 위에 `concepts/` 공통 개념 초안을 구성했다. `seeds.py`의 8가지 역할(분야·지표·대상·분류·공간·시간·식별·단위), 16개 탐색 분야와 88개 개념은 프로젝트 작업용 정의다. 분야 배치 inTopic 72개·구분 제안 distinguishFrom 7개는 상하위 클래스·자료 결합·통계적 관계를 확정하지 않는다. 사람 승인 의미 매핑·통계 관계는 0개다.
139. `concepts/build.py`는 10:31:40 UTC 개념 스냅샷 생성 당시 등록정보 440,356개·SQL 명세 행 1,442,676개를 읽기 전용 트랜잭션으로 조사했다. 제어 항목 행 68,403개는 분석 개념 후보에서 제외하고 기존 수집에는 보존했다. 511,089개 용어 묶음은 제공처·역할·표기별 집계이며 고유 개념 수가 아니다. 원분류 7,839·명세 항목명 349,927·단위 표기 653·키워드 후보 152,670개다. 후보 출현에는 기관별 표준 연결 반복이 포함되며 전국 수집/온톨로지 완료율은 산출하지 않는다. 실시간 수집 인덱스와 고정 개념 스냅샷의 수량은 서로 다를 수 있다.
140. 검토 연결 예시 703개는 명세 항목 310개와 원분류·키워드 393개다. 모든 88개 개념에 최소 하나의 표기 근거가 있지만 의미 승인을 뜻하지 않는다. 명세에는 원문 설명·자료형·단위·코드 분류 표본·수집 근거·저장 문서 위치를 연결했다. 원문 위치가 기록되지 않은 78개 예시는 임의 원문 좌표를 만들지 않고 추출 문서의 /fields/ 위치와 미기록 여부를 분리한다. 별도 버스-생활인구 결합 검토 1건은 실제 결합이나 통계 분석을 실행하지 않은 후보다.
141. `/concepts`에서 개념 검색·역할 필터·클릭 가능한 관계 지도·정의/검토 조건·실제 칼럼/목록 출처를 볼 수 있다. 칼럼 명세 근거를 먼저 표시한다. 원문 용어 검색은 읽기 전용 보조 SQLite를 사용하며 80개씩 제한하고 연속 요청의 오래된 응답이 최신 결과를 덮지 않게 했다. 원문 용어·JSON·RDF·검토 목록을 내려받을 수 있다. 기존 자료 검색과 ?record= 상세 링크로 왕복한다. `model.json`은 concept_layer와 instance_files로 별도 개념 JSON을 참조한다. 기존 인덱서가 이 추가 참조를 보존하는 것도 확인했다.
142. `review-queue.json`에는 검토자·판정·이유·출처·범위를 기록한다. 재빌드 중 표본에서 빠졌다 다시 나타난 항목의 판단도 유지하며 중복 보존 항목을 만들지 않는다. 판단이 모델에 자동 승인되는 기능은 없다. `concepts/check.py`의 [8개 검증 그룹](concepts/validation-report.json)은 703개 매핑과 실제 DB/정의 위치 대조, 원문 294개 SHA256, 용어 511,089행 JSONL/SQLite 전체 왕복, RDF 및 HTTP 검색·내보내기·오류 경로, 검토 판단 보존을 확인했다. 이는 의미·결합·통계 검증이 아니다. [화면 검사](concepts/ui-check.json)는 학교 코드·감염 용어 검색·연속 검색·자료 상세 왕복·브라우저 오류 0개를 확인했다. 관찰한 304px 창에서 페이지 가로 넘침은 없고 그래프는 내부에서 스크롤한다.
143. 11:03 UTC 재개 시 로컬 검색 서버와 기존 워커가 없어 `resume_runtime.ps1`로 저장 설정의 미완료 큐만 복구했다. 중단 원인은 미확인이다. FILE 22380·KOSIS 8548·인덱서 25476·브라우저 21672·감시기 10808이며 다음 실행에는 PID 대신 실제 명령행도 대조한다. 소진된 유한 큐는 재실행하지 않았다. 개념 초안 생성은 종료됐고 기존 수집은 계속된다. [이번 구성 기록](concept-layer-20260914-report.json)을 참고한다. 개념 작업은 로컬 파일에 반영했으며 Git 공개나 새 AWS 서버 배포는 수행하지 않았다. 다음 단계는 원문 용어에서 개념을 확장하고 사람이 개념 매핑의 의미·범위를 검토하는 것이다.


144. 2026.09.14 사용자의 “전체 지식 그래프, 어느 포털에서 어떤 개념을 가져왔는지 다 연결” 요청에 따라 `/concepts/graph`를 추가했다. 포털–공통 개념 전체 개요와 포털→등록자료→원문 항목→공통 개념 후보 경로를 함께 제공한다. 개념은 프로젝트의 88개 작업용 정의이며 포털이 같은 온톨로지를 공식 제공한다는 뜻이 아니다. 기존 703개 검토 예시를 늘려 보인 것이 아니라 새 읽기 전용 DB 스냅샷에서 모든 표기 연결을 다시 산출했다.
145. `concepts/build_graph.py`의 스냅샷은 등록정보 440,356개·원분류/키워드 출현 2,008,637개·명세 행 1,473,095개·목록 출력 토큰 640,859개를 보존한다. 원문 항목 합계 4,122,591개, 용어 묶음 720,861개, 개념 연결 출현 260,507개(매핑된 원문 항목 257,597개)다. 원문 항목 3,864,994개는 현 개념에 연결되지 않아도 검색한다. 이 숫자들은 고유 분석 변수·고유 개념·전국 수집 완료율이 아니다. 기관별 표준 명세 연결과 등록/분류 반복을 포함한다. 명세 중 제어 항목 68,403개는 매핑 규칙에서 제외했다. 이전 511,089개 용어 집계와 달리 새 범위에는 출력 토큰·제어 표기도 들어가며 스냅샷 시각도 다르다.
146. 32개 수집 포털을 노드로 표시하고 이 중 21개에서 88개 공통 개념과 433개 포털–개념 쌍(근거 종류별 619개 집계)이 있다. 나머지 11개는 현재 매핑 규칙의 연결이 없으며 임의 분야/동일성 관계를 만들지 않았다. 조사 후보 125개 모두를 토글로 표시할 수 있지만 미수집 93곳에는 등록정보나 개념 연결을 생성하지 않는다. `등록 자료` 보기로 개념/항목 유무와 무관하게 440,356개 등록 전체를 페이지별 탐색한다. 부산 12,551개처럼 등록은 있지만 그래프 항목을 추출하지 못한 자료도 포털→등록정보 경로로 확인한다.
147. 원문 항목 그래프는 24개씩 표시하지만 조회 인덱스는 표본 제한 없이 전체를 보유한다. `graph_api.py`는 개요·등록정보·출처 경로·항목 상세만 읽기 전용으로 제공한다. 포털/개념/근거 종류/항목명 또는 자료 이름을 필터링하고 기관명·기관 ID·공식 URL·조회 시각·원문 SHA256·칼럼 순번/위치를 확인한다. 제공기관은 포털의 원래 식별자를 보존하며 포털 간 동일 기관으로 자동 합치지 않는다. 큰 화면·확대/축소·이동·노드 선택·전체 화면 페이지 이동·선택 해제를 제공한다. 전체 관계가 승인됐거나 실제 데이터를 결합·상관 분석했다는 뜻은 아니다.
148. 전체 인덱스는 `.local/domestic-catalog/knowledge-graph.sqlite3` 약 1.42GiB로 분리했다. 원본 catalog.sqlite3는 빌더가 수정하지 않는다. 빌드 중 제한된 용어 캐시와 SQLite를 사용하며 브라우저는 개요와 필요한 페이지만 읽는다. `graph-overview.json`은 포털/개념과 경로 집계이며 수백만 노드를 한 JSON으로 직렬화한 파일은 아니다. root model.json의 knowledge_graph_layer와 instance_files가 새 그래프를 참조한다. 원본의 미리보기 헤더 후보·요청 인자·비공개 DB·실제 관측값은 새 그래프 원문 항목 집계에 포함하지 않는다. 가공된 숫자 항목 ID는 스냅샷 내부 식별자이며 장기 검토에는 원본 등록/근거 ID·순번·원문 위치를 함께 사용한다.
149. [전체 그래프 검사](concepts/graph-validation.json) 6개 그룹은 모든 등록·분류/키워드 2,008,637개·명세 1,473,095개·출력 후보 640,859개를 원본 DB와 대조하고, 4,122,591개 항목에서 모든 260,507개 매핑을 재계산했다. 모든 집계와 참조, 검색·페이지 경계·리터럴 쿼리 검사를 통과했다. [등록 자료 검사](concepts/graph-registration-check.json)는 전체 등록 수와 부산의 처음/마지막 페이지, 항목이 없는 자료의 보존, 자료별 항목 필터를 확인했다. [화면 검사](concepts/graph-ui-check.json) 10개는 캔버스 KOSIS 선택(52개 개념), 실업률 40개 경로의 24+16 페이지, 원문 근거 표시, 부산 등록 조회, 감염 미연결 검색 781개, 후보 125개 표시·지표누리 연결 0개, 브라우저 오류 0개를 확인했다. 이는 출처·표현 검증이며 사람의 의미/통계 승인이 아니다.
150. 그래프 API 반영을 위해 기존 browse.py의 실제 명령행을 확인하고 검색 서버만 재시작했다. 현재 브라우저 PID는 17992이며 로그는 browser-graph-20260914T114601.*.log다. FILE 22380·KOSIS 8548·인덱서 25476·감시기 10808은 유지했다. 최신 check_runtime 결과와 실제 명령행을 다음 작업에서 재확인한다. 개념·그래프는 각각 고정 스냅샷이며 기존 수집은 계속된다. [그래프 사용법](concepts/GRAPH-README.md)을 참고한다. 이번 변경은 로컬 구성이며 Git 공개·AWS 배포·타인 메시지 발송은 하지 않았다.


## 2026-09-14 GitHub 전체 지식 그래프 공개 공유

- 사용자 명시 요청으로 `origin`의 `codex/knowledge-graph-sharing-20260914` 브랜치를 생성·게시했다. 커밋 `307c7f4671ab86699d8be77fbb3d33b83e00db33`. main은 `cac9c1caebea38950beaf2bbed69fadfe18469fc` 그대로다.
- 공개 실행 URL: https://chlsuun.github.io/public-data-ontology/ . Pages 원본은 위 브랜치 `/docs`, HTTPS 활성화, 첫 배포 성공. 저장소 About에도 URL 등록.
- 전체 정적 공유 데이터: 원문 항목 4,122,591개, 등록정보 440,356건, 매핑 출현 260,507건, 조회 영수증 115,904건. 1,170개 gzip 파일, 73,842,162바이트. 표본 제한 없음. 스냅샷은 2026-09-14T11:25:59.731267+00:00.
- `scripts/build_full_graph_share.py`(로컬 DB→전체 압축 조각), `render_full_graph_share.py`, `check_full_graph_share.py`, `check_shared_graph_queries.mjs` 추가. docs/index.html, concepts.html, graph-worker.js, graph-static.js가 공유 화면.
- 모든 공유 행을 고정 SQLite와 대조, 전체 파일 SHA-256, 검색 비교 17개 검사 통과. 공개 배포에서도 17개 검사 통과 및 6개 주요 HTTP 파일이 Git 객체와 일치. 공개 브라우저 KOSIS 실업률 40개 조회, 오류 로그 없음.
- 개념 미연결 항목 및 0항목 등록자료도 보존. 필터/보기/페이지/미수집 후보 선택 URL 복원 지원. 개념 연결은 여전히 사람 검토 전 후보, 승인 0건.
- 수집 중인 원문 증거·중간 inventory·SQLite는 Git 제외. 실제 공유 데이터는 docs/graph-data에 완결되어 있고 로컬 수집 문서는 로컬 경로도 포함한다. 현재 수집기가 바꾼 모델/수집보고서/등록목록은 게시 이후의 미커밋 작업으로 보존했다. 이를 현재 공유 스냅샷과 혼동하지 말 것.
- 테스트용 정적 HTTP 서버 8767은 종료, 기존 browse.py 8766 및 수집기는 유지.
- Git 커밋 후 자동 maintenance가 기존 loose 객체 535,174개(약 5.3GiB)를 정리하며 오래 걸렸음. 커밋·push 성공 확인 후 해당 커밋이 시작한 pack-objects 하위 프로세스만 중단. Git 파일/객체 삭제 없음. 향후 게시에서 자동 유지보수 지연이 발생하면 `git -c maintenance.auto=false ...`로 해당 명령의 자동 정리를 피할 수 있다.
- 새 Git 게시/Pages 갱신은 사용자의 명시적 Git 반영 요청 때만 한다. 다른 프로젝트 및 SSH/AWS 서버는 변경하지 않았다.


## 2026-09-14 국내 수집 계속 · 국가법령정보센터 확장

- 사용자 `계속진행해`에 따라 기존 FILE·KOSIS 수집을 유지하고 국가법령정보센터 LAW OPEN DATA를 추가했다. [이번 진행 기록](continuation-20260914T1322-report.json), [수집 설명](LAW-README.md). Git 게시·다른 프로젝트·AWS 변경·타인 메시지 발송 없음.
- `collect_law.py`는 공식 guideList.do의 실제 openApiGuide(htmlName) 링크 **195개**와 공개 POST 안내 문서를 모두 확보한다. 사이트 표시 총계 **191개**와 다르므로 총계 대조 완료는 아니다. 안내 ID는 API 기능의 문서 식별자이며 개별 법령·판례 수가 아니다. 기존 `law` 포털 ID를 사용했고 새 중복 제공처를 만들지 않았다.
- 응답 명세가 있는 안내 **180개·3,210항목**, 별도 요청 인자 **1,968항목**. 응답 표가 없는 안내 **15개**는 `response_definition_not_observed`로 유지하며 elsewhere 명세 부재나 not_applicable로 단정하지 않는다. 법령 내용/샘플 API는 호출하지 않았다. 법제처는 안내 게시 기관이며 원 생산기관 동일성은 미확정이다.
- 파서 v1의 누락 원인을 저장 원문으로 검증해 v2로 보완했다. 출력 제목에 요청변수 머리글이 붙은 6개 안내, 빈 caption의 명시적 요청/필드 머리글, 빈 tr 1개를 처리한다. previous_parser_attempt를 보존하고 같은 이름의 응답 행은 원문 순서대로 각각 저장한다. 목록의 잘못된 span으로 폭이 6이 된 9행은 분류를 임의로 보정하지 않았다. [목록 QA](law-catalog-report.json), [사이트별 QA](law-source-qa.json).
- [원문 검사](law-source-check.json) **9개 그룹**은 목록과 모든 195개 안내의 SHA256·바이트 **196건**, 실제 링크/순서/표 셀/요청·응답 분리/반복 라벨/미해결 상태를 대조했다. [DB·HTTP·내보내기 검사](law-import-check.json) **7개 그룹**은 195개 등록·3,210행·모든 문서 메타데이터, 실제 검색 195/정식 명세 180, 정상·머리글 충돌·미확보 사례, 진행·현황을 확인했다. 전체 16개가 통과했으며 다른 포털을 전수 재검증했다는 뜻은 아니다.
- 기존 증분 인덱서가 v2를 자연 반영했다. 전체 DB 재구축이나 인덱서 중복 실행 없음. `inventory/law-columns-snapshot.jsonl.gz`는 3,210응답행 전체와 DB를 대조했다. 국가법령정보센터 이름/진행 표시와 공식 안내 목록을 여는 링크를 검색 화면에 반영하고 JavaScript 문법·실제 HTTP HTML을 확인했다. 개별 근거 링크는 POST 상세를 잘못 GET으로 여는 대신 공식 목록과 안내 ID·원문 행을 함께 제공한다. 실제 브라우저 클릭 검사는 이번 범위에 포함하지 않는다.
- ITS 공개 소개 조회는 인증서 체인 검증 실패로 미해결이다. [접근 QA](its-access-qa.json)에 시각·URL·실패 이유를 보존했고 TLS 검증을 해제하지 않았다. 이 실패를 카탈로그 확보/서비스 폐쇄로 분류하지 않는다.
- 2026-09-14T13:18:51.875548+00:00 UTC 스냅샷: 후보 125곳 중 목록 확보 **33곳**, 등록 **440,551행**, 명세 문서별 항목 **1,328,725개**. 제공기관별 표준 연결 반복까지 포함한 SQL 항목은 **1,577,587행**으로 서로 합산하지 않는다. 전국 모집단과 전체 칼럼은 미완료이며 의미 승인·통계 분석은 0건이다.
- 최신 FILE 처리 69,700/83,691, KOSIS 처리 64,325/266,688이며 실패를 포함하는 처리 수다. [workers-20260914T1322-check.json](workers-20260914T1322-check.json)에서 FILE 22380·KOSIS 8548·인덱서 25476이 각각 하나이고 현재 오류 로그 0바이트, supervisor·검색 서버 정상, stop 표시 없음. 유한 LAW 큐는 자연 종료했으며 반복 워커에 등록하지 않았다. 다음 재개에는 PID와 실제 명령행·진행 시각을 재확인한다.
- 공개 및 로컬 전체 그래프는 11:25:59 UTC의 고정 스냅샷을 유지한다. 새 LAW 수집은 실시간 검색 DB에 반영됐으며 그래프나 GitHub Pages의 자동 갱신으로 혼동하지 않는다. 새 Git/Pages 반영은 명시 요청 때 진행한다.


## 2026-09-14 13:52 UTC 후속 수집 · FISIS 진행 중 체크포인트

- 국내 수집 자동화 후속 실행에서 FILE 22380·KOSIS 8548은 정상 작동했다. 기존 인덱서 25476이 13:47 부근 Windows WinError 5(원자적 domestic-portals.json 교체)로 종료됐으나 supervisor가 2316으로 복구한 기록을 확인했다. 운영 원인은 미확정이다.
- common.dump에 Windows 5/32/33일 때만 최대 1.55초·6시도의 원자적 교체 재시도를 넣었다. 기존 JSON을 덮어 잘라 쓰지 않으며 영구 실패는 그대로 예외다. [4개 검사](atomic-write-retry-check.json)는 실제 Windows 삭제 공유 없는 읽기 핸들에서 WinError 5 세 번 후 정상 교체까지 확인했다. 2316을 소유 표식 indexer.stop으로 정상 종료시킨 뒤 표식을 제거했고 기존 supervisor가 **28112**로 재개했다. [복구 기록](indexer-atomic-recovery-20260914.json). FILE/KOSIS 재시작·기본 DB 재구축 없음.
- `collect_fisis.py`로 FISIS 공개 단수통계표 선택기의 권역 **22개·분류 경로 241개**를 모두 순회했다. 카탈로그 경로 출현 **1,205개**, 고유 FORM_NO **736개**, 원문 수록기간별 조회 대상 **736개**다. 공식 포털 총계는 없으며 전체 완료 아님. 카탈로그와 정의서 큐는 inventory/fisis-catalog.json, fisis-catalog-paths.json, fisis-definition-queue.json에 보존했다.
- `collect_fisis_api.py`는 인증키 필요한 API를 호출하지 않고 설명서 **4기능·응답 41행·요청 19행·설정 8행**을 수집했다. 통계목록 API 분류표의 마지막 rowspan=2가 실제 행을 초과해 원문을 보존하고 미해결 표시했다. API listNo와 UI FORM_NO 등 코드 체계를 자동 동일시하지 않는다.
- `collect_fisis_companies.py`는 별도 금융회사 선택 화면의 권역 **16개**·각 최신월 **202603**의 선택 노드 **1,468행·코드 1,466개**를 확보했다. 분류/집계 노드와 회사가 섞인 원문이므로 회사 수·공공기관 제공처 수로 합산하지 않으며 providers.json을 바꾸지 않았다. [5개 검사](fisis-companies-source-check.json)가 원문 **18건**과 모든 행/코드를 대조했다.
- FISIS 통계표 항목·용어 해설 수집은 유한 736큐로 진행 중이다. 현 프로세스 **13112**, 로그 `fisis-v2-20260914T141600.stdout.log` / `.stderr.log`, 상태 `.local/domestic-catalog/fisis-worker.json`. 현재 420/736이다. 이 워커는 supervisor workers.json에 등록하지 않았다. 후속 실행 시 **실제 명령행을 확인해 중복 시작하지 말고**, 종료됐는데 queue_exhausted=false이면 `python -X utf8 -u ontology-prototype/domestic-catalog/collect_fisis.py`를 숨김 실행해 저장 지점에서 재개한다. 저장 원문은 재사용한다.
- v1→v2 수정은 DATA_TYPE이 물리 DB 자료형이 아닌 출처 코드(공개 JS에서 5를 비율 표시로 처리)임을 반영한다. datatype=null, source_data_type_code와 CAL_UNIT 원문 코드를 따로 보존하며 단위를 추정하지 않는다. 기존 FISIS 프로세스 26780을 소유 표식으로 정상 종료한 뒤 **13112**로 재개했고 기존 원문으로 재파싱했다. 이전 파서 요약은 previous_parser_attempt에 보존한다.
- 응답 columnCount와 실제 열 설명 수가 다르면 `statistical_items_observed_columns_partial`로 유지한다. 메타데이터 HTTP 500은 별도 metadata_fetch_unresolved이며 확보한 행 항목을 버리지 않는다. 용어해설은 별도 원문 엔트리이고 항목과의 의미 동일성·통계관계는 승인하지 않는다. 관측값/엑셀/API 인증요청 없음.
- [FISIS 설명](FISIS-README.md), [사이트별 QA](fisis-source-qa.json), [원문 검사](fisis-source-check.json)는 검사 당시 정의 문서 수를 명시한다. 워커의 새 문서와 구분한다. **최종 큐 종료 후 check_fisis.py --import로 전체 DB·HTTP 대조와 항목/용어해설 압축 export가 필요하다.** 아직 검증되지 않은 새 수집을 전수 검사 완료로 보고하지 않는다.
- 검색 HTML에 FISIS 이름·진행·원문 용어해설을 추가했고 텍스트를 이스케이프해 표시한다. HTTP HTML 일치/JS 문법 검사는 통과했으며 실제 브라우저 클릭 검사는 하지 않았다. 공개/로컬 전체 그래프는 기존 고정 스냅샷이며 Git 게시·AWS·타인 메시지 발송 없음. 이 항목은 진행 중 체크포인트이고 아래 최종 보고가 있으면 그 보고를 우선한다.


## 2026-09-14 FISIS 확장 최종 기록 · 위 진행 중 체크포인트 갱신

- [최종 후속 수집 보고](continuation-20260914T1433-report.json). 14:28:44 UTC에 **736개 통계표 큐를 전부 한 차례 조회**했고 유한 수집기 13112가 자연 종료했다. 정상 완료된 이 큐를 supervisor에 반복 등록하거나 다시 시작하지 않는다. 전국 수집은 계속 미완료다.
- 통계 행/열 항목 **19,562개**, 별도 공식 용어 해설 **2,707개**다. 열 설명 부족 **734개**, 항목 정의 관측 **2개**, 메타데이터 문서 관측 **698개**, HTTP 500 **38개**를 각각 보존했다. 원문별 항목/용어 반복을 포함하며 상관분석 변수 수나 승인된 개념 수가 아니다. API 문서 **4개·응답 41행·요청 인자 19행·설정 8행**과 합쳐 등록정보는 **740개**다.
- [원문 대조](fisis-source-check.json) **11그룹**은 22권역·241분류 경로·1,205목록 출현→736FORM_NO/기간큐, 모든 736개 문서의 행/열 항목·용어 해설·표 위치, 4개 API 응답 명세와 잘못된 분류 rowspan을 대조했다. 조회 영수증 **1,740개** 중 정상 원문 **1,702개**의 SHA256·바이트를 검증했고 실패 **38개**는 HTTP 500으로 별도 보존했다. 실패 문서에 해시나 명세를 만들어 넣지 않았다.
- [DB·HTTP·내보내기 대조](fisis-import-check.json) **6그룹**, 합계 **17그룹**이 통과했다. 모든 740개 등록, 736개 통계 문서와 4개 API 문서 메타데이터, **19,603개** 명세 행이 원문 추출 결과와 일치한다. API 41행은 통계 항목 19,562행과 구분한다. 실제 HTTP 검색 740건·임직원현황 항목/해설을 확인했다.
- 휴대용 내보내기 `inventory/fisis-audited-columns-snapshot.jsonl.gz` **19,603행**, `inventory/fisis-glossary-snapshot.jsonl.gz` **2,707행**을 전부 다시 읽어 대조했다. 용어 해설에는 통계표·기간·원문 URL·evidence_id·행 위치와 미승인 상태를 붙였다. 모델은 이 파일들과 QA/설명을 참조한다.
- 별도 회사 선택 목록은 앞서의 **16권역·202603·1,468행/1,466코드**이며 [5개 원문 검사](fisis-companies-source-check.json)가 통과했다. 기존 providers.json이나 개별 데이터셋 수에 이를 합산하지 않는다. 회사 코드의 업권 간 중복·집계 노드, 과거 시점 범위는 미확정으로 보존한다.
- DATA_TYPE은 출처 코드로 분리한 v2가 736개 전체에 반영됐다. 공개 JS의 code 5 비율 처리 근거를 보존하고 물리 자료형·통화 단위를 임의 추론하지 않았다. 기존 원문을 재사용했으며 API 인증키·회사 재무값·통계 관측값·엑셀 생성 요청은 하지 않았다.
- [화면 소스 검사](fisis-browser-source-check.json)는 실제 HTTP HTML, FISIS/회사 선택 목록 이름, 세 수집 보고서, 용어 해설 이스케이프·원문 위치 표시를 확인했다. 최종 JS 문법 검사 통과. 브라우저 클릭 동작의 시각 검사는 이번 범위가 아니다.
- 저장 오류 보완은 [실제 Windows 공유 잠금 포함 4검사](atomic-write-retry-check.json)가 통과했다. [복구 기록](indexer-atomic-recovery-20260914.json)의 새 인덱서 **28112**가 정상 작동한다. check_runtime.ps1은 이제 별도 FISIS 유한 워커도 실제 명령행과 로그로 확인하며, 큐 소진 후 프로세스 0개가 정상 상태다.
- 2026-09-14T14:29:46.548733+00:00 UTC 기준 국내 후보 **125곳** 중 목록 확보 **34곳**, 등록 **441,291행**, 명세 문서별 항목 **1,402,533개**. 제공기관별 표준 연결 반복 포함 SQL 행 **1,651,395개**는 앞 수량과 합산하지 않는다. 포털 전체 명부·자료 칼럼은 미완료이며 의미 승인·통계 분석은 0건이다.
- [14:31 런타임 검사](workers-20260914T1431-check.json) 통과. FILE **22380**(75,050/83,691), KOSIS **8548**(68,550/266,688), 인덱서 **28112**, 기존 supervisor/검색 서버 정상이고 중복·stop 표식·현재 stderr 오류 없음. 처리 수에는 미해결이 포함된다. 다음 실행에는 실제 PID/명령행·최신 진행을 재확인한다.
- 새 제공처·기존 미확보 명세 조사를 계속하며, FISIS 734개 열 정의/38개 오류와 LAW 총계 불일치/15개 명세 미관측도 미해결 목록으로 유지한다. Git 커밋/푸시/공개·AWS·다른 프로젝트·타인 메시지 발송 없음. 공개/로컬 전체 그래프는 이전 고정 스냅샷을 유지한다.

## 2026-09-15 00:12 KST — 충분성 판단에 따른 수집 종료 확인

- 수집기·인덱서·감시자는 강제 종료 없이 정상 종료. 검색 서버는 유지. `workers-20260914T1512-check.json` 통과.
- 최종 등록정보 441,291건, 원문 명세 항목 1,427,860회, 등록 연결을 포함한 칼럼 출현 1,676,722회. DB·HTTP·압축 내보내기 수량 일치. 마지막 증분 확인: 미반영 변경 0, 파싱 오류 0, 연결 실패 문서 0.
- FILE 미처리 6,087개, KOSIS 미처리 196,098개 및 기존 오류는 그대로 보존. 큐 소진이나 전국 수집 완료로 변경하지 않음.
- `collection-stop-report.json`, `COLLECTION-SUFFICIENCY.md`, 정책 v2.0 및 model.json의 collection_decision을 먼저 읽을 것. 예약 자동화 삭제 완료. 새로운 Git 공개는 하지 않음.
